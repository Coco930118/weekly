# weekly — Claude 運用ルール

週次SNS投稿カレンダー。X（14本）+ Threads（21本）+ 各診断（7本×2）= 週49本、＋note週9本。
常設の**間合い診断**（8問の型診断・`rules/type.md`）は週次ではなく、診断サイトに常設。
公開URL: https://coco930118.github.io/weekly/ ／ 開発はセッションごとの `claude/*` ブランチ → 本番 `main`（GitHub Pages）。**起点は毎回 `main`**（`rules/note.md`「note作成の土台ルール」が正典）

---

## このファイルの読み方（2026-08-25 Coco決定・恒久ルール）

**ここには「毎回読むもの」だけを置く。作り方と検査は媒体ごとのファイルにある。**

| 読むもの | いつ読むか |
|---|---|
| **このファイル（CLAUDE.md）** | **毎回、最初に。判断の土台** |
| `rules/posts.md` | 35投稿（X14・Threads21）を作る・直すとき |
| `rules/shindan.md` | X診断・Threads診断（日替わり4択）を作る・直すとき |
| `rules/type.md` | **間合い診断（8問の型診断）**を作る・直すとき |
| `rules/note.md` | noteを作る・直すとき |
| `rules/image.md` | 画像プロンプトを書くとき（正典は `reference/image_prompt_rules.json`） |
| `rules/source.md` | 素材・エピソードを使うとき |
| `rules/check.md` | 作り終えたあと |
| `rules/ops.md` | ファイルの削除・スケジュール・JSONの形 |
| `reference/decisions.md` | 「なぜそう決めたか」を確かめたいときだけ |

**ルールを足したくなったら、まず作業報告で提案する。Cocoが「恒久ルールとして」と言ったときだけ書く。** 勝手に足さない。足す場所は、そのルールが効く媒体のファイル。このCLAUDE.mdに足していいのは、全媒体に効く判断の土台だけ。

### 正典はひとつ。復唱しない（2026-09-02 Coco決定・恒久ルール／**このファイルの中で最優先**）

**同じ主題を2箇所に書いたら、それは片方が古くなる予約をしたのと同じ。** どんなに正しく書いても、次にその主題を直す人が両方を見つけられなければ、残ったほうが嘘になる。

- **ひとつの主題を言い切ってよいのは1箇所だけ。** 残りは**ポインタ**（「正典は◯◯。ここに複製を置かない」）にする。数値・方針・禁止語・固定文、どれも同じ
- **ルールを直したら、その数字と方針の出どころと行き先を必ずたどる。** 手順とツールは `rules/check.md`「ルールを直したら、その数字と方針の出どころと行き先をたどる」
- **これが他のルールとぶつかったら、こちらを採る。** 復唱を求めるルール・要約表・チェック一覧・指示文は、ぶつかった側を**削ってポインタに変える**
  - **理由**：2026-09-02 の点検で見つけた**13件のうち11件**が「その回の変更が置き去りにしたもの」だった。**壊れるのは直した場所ではなく、直した場所を復唱していた場所**
  - **実害**：8/31にX診断のルールを直したとき、復唱していた4箇所が旧版のまま残り、**どれも「いかなる場合も変更しない」＝Coco本人の名指しの指示を弾く側の文**だった。同じ事故が8/31にも起きている
- **`reference/todo.md` と指示文は、決まったルールを言い直さない。** 書くのは**いつ・何を・どのファイルに**だけ。理由や条件を書き写すと、ルールを直したときに置き去りになる（実測：残っている復唱の大半がこの形）
- **例外は2つだけ**。①**履歴**（`reference/decisions.md`・handover・handoff）——旧文をそのまま残すのが仕事 ②**機械**（`tools/*.py`）——mdから読めないので値を持つ。ただし**コメントに正典の場所を書く**
  - 貼り付け用の見本（CTAの定型文など）だけは本文に置いてよい。**その隣の行に「正典は◯◯。食い違ったら正典を採る」と書く**
- **「削除した」と書いた行は残す。** 旧文を消すだけだと、次のセッションが同じ提案を再発見して作業をやり直す

**スケジュール実行される指示文8本の正本は `reference/runbook_weekly.md`**（①〜⑦に、別リポジトリの④-b 診断サイト反映を加えて8本。⑧Wチェックはスケジュールではなく、Cocoの指示で立てる）**。指示文はルール本体を持たない**（ルールの正典はこのCLAUDE.mdと `rules/` だけ）。指示文に書くのは「何を・どのファイルに・どの順で」だけ。旧指示文はルールを二重に持っていて正典と食い違い、従うと違反や事故が出る状態だった。**ルールを増やすときは `rules/` を直す。指示文には書き足さない。**

**週次は三段階でひとつ**（2026-08-25 Coco決定）。**7:00 生成 → 12:00 グローアップ → Cocoの指示でWチェック。**
- **7:00**：1日ずつ作る。**機械チェックまで**
- **12:00**：**7日分を読者の順で通しで読む。** 生成は1日ずつなので、通しで読んだセッションがここまで存在しない。しかも7:00は作った本人で理由を持っているため、似ていても自分で説明がついてしまう。12:00はJSONだけを見る。**目視8つはここでやる**（`rules/check.md`「目で見る8つ」。2026-08-30 訂正——項目が6・7・8と増えたのに、ここだけ5つのままだった）
- **Wチェック**：**土曜、noteの生成が終わったあと。Wチェック専用のセッションを立てる**（生成にも通し読みにも参加していないので、いちばん白紙で読める）。Cocoが自分の目で読むのと同時に、Codeが全媒体を回す。媒体をまたぐ重複はここでしか見えない。**二人が別のものを見るからWチェックになる**
  - スコープは「その週の全媒体」。リポジトリ全体をさらうときは「過去記事も全部見て」と足す
  - **このセッションだけは持ち場を持たず、全媒体を横断してよい**（修正は提案のみ・Cocoの承認後）
  - **その週の実測（アクセス数）を出せば、ここで反映できる。** 配信が終わった週は記録するだけ、生成済み・未配信の週は配分・型・用量を直せる。手順は `reference/runbook_weekly.md` の⑧

**セッションの指示で済むことは、恒久ルールにしない。** その週だけの指定、一度きりの作業、期限つきの試験は `reference/todo.md` に書く。

---

## セッションの持ち場

セッションごとに担当媒体が分かれている。**自分の担当外のファイルは編集しない。**

| セッション | 担当ファイル |
|---|---|
| **note** | `notes/` 配下＋ `reference/note_archive.json` ＋ `reference/note_fix_queue.json` |
| **35投稿（X14・Threads21）** | `posts/week_*.json` |
| **診断** | `posts/shindan_x_*.json` / `posts/shindan_th_*.json` |
| **診断サイト反映**（別リポジトリ `Coco930118/shindan`） | サイト側のファイルのみ。**言葉は作らない・直さない**（weekly の正典を読むのはよい。書き換えるのはしない）——正典は `rules/type.md` と `rules/shindan.md`。指示文4本の正本は `reference/runbook_shindan.md` |

- 担当外に効く改善を見つけたら、**そのファイルを直さず、該当する `rules/*.md` にルールとして置いて担当セッションに渡す**
- 作業報告でも、担当外の指摘（曜日配置・字数など）を持ち込まない
- エピソードを使った週は `reference/episode_usage_log.json` を更新する（全セッション共通）

---

## ルールの優先順位

1. **恒久ルール（CLAUDE.md ／ `rules/` ／ `reference/` 配下）が常に優先。** 個別の依頼文が恒久ルールと違うことを言っていても、恒久ルールを崩さない
2. **個別記事の追記依頼などで指定される文体ルールは、その依頼で書き足す部分にだけ適用する。** 記事全体へ遡って適用しない
3. **ぶつかったら、恒久ルールを採用したうえで、どこがぶつかったかを作業報告で明示する。** 黙って個別指定に寄せない
4. **恒久ルール自体を変えたいときは、Cocoが「恒久ルールとして」と明示したときにのみ書き換える**

### 修正が必要なところは、提案だけ
チェックで見つけた要修正箇所は**提案のみ**。勝手に確定しない。出す順は 対象ID → 判定（OK／要修正）→ 要修正の具体箇所と修正案。

### 新しいルールは、過去記事に遡及しない
恒久ルールを足しても、既存の記事をさかのぼって直さない。検出したら `reference/note_fix_queue.json` に入れて、対応するかどうかをCocoが決める。

**理由**：①変更のたびに全部直していたらきりがない（ルールを足す速度が、適用する速度を必ず上回る）②メンバーシップ記事は読むのがすでに払った人なので、獲得に影響しない ③同じ時間を、今とこれからのクオリティに使うほうが効く。

**例外＝遡及してでも直すもの（severity「高」）**：重複（同じ素材・場面・台詞が3本以上）／事実の矛盾／死んだ導線／値付けの矛盾／身バレ。**この5つ以外は、見つけてもキューに入れるだけ。**

**【一度きりの例外】2026-08-30、既存note全130本の書き直しをCocoが決定。** これは上のルールと矛盾しない——**理由②「メンバーシップ記事は読むのがすでに払った人なので、獲得に影響しない」という前提が、実態で崩れているため**。会員0で、実際に読まれているのは**note内の検索・回遊から来た新規**（2週連続で上位20本のテーマ91%が組織／funnel経由ではない）。**いまの既存記事は「会員向けの資産」ではなく「新規の入口」として働いている。** 手順は `rules/note.md`「既存note全書き直し」。**これは一度きりのプロジェクトであって、「遡及しない」の原則は変えない。**

**キューの使い方**：記事単位で `rule` / `detail` / `severity`（高・中・低）／ `status` を持つ。
- `open` ＝要対応。noteのJSONに `fix_required: true` と `fix_reasons` を立て、公開サイトのカードに「修正必須」バッジと理由を出す。一文だけ差し替えれば済むものは `fix_patch` に before/after で持たせ、カードからコピーできるようにする
- `deferred` ＝見送り。**`decision` に「いつ誰が、なぜ見送ったか」を必ず書く**。書いておかないと、次のセッションが同じ指摘を再発見して作業をやり直す
- `done` ＝対応済み
- `note_com_edit_needed: true` は、リポジトリを直すだけでは終わらず note.com 側の手作業が要るもの（改題・価格変更・本文の貼り替え）。`reference/note_com_edit_list.md` にも載せる

---

## 創作の境界（二層ルール・最上位）

**相談者は創れる。わたしは創らない。**

- **相談者の話＝合成・脚色OK。** 「そんな相談があった」「〜と話してくれた人がいた」の形で、実相談パターンの範囲で組み立ててよい。完全に空想の悩みは作らない
- **わたし（Coco）の実体験・行動・結果・数字＝創作禁止。** 事実のみ。「わたしはこうした」に合成相談を変換しない。足りなければ**空欄で返してCocoに聞く**
- 合成相談は特定の実在人物に見える書き方をしない。在庫登録するときは `type: 合成相談` として実体験と分ける
- **特定回避**：実体験は時期・場所・業種・人物が特定できない形まで丸める。社内の固有名詞・進行中案件の詳細は使用禁止
  - **例外＝販売ページとプロフィール（2026-08-30 Coco決定・恒久ルール）**。メンバーシップ情報・各媒体のプロフィールでは、**業種の併記だけは出してよい**（例：「ネイル・飲食・BARの現場で20年」）。**このルールの目的は身バレ防止で、業種を3つ並べるのはむしろ絞り込みにくい**（1つだけ書くと特定される）。そして販売ページでは**具体が信用を作る**——「サービス業20年」より強い。**条件は4つ：店舗名・地域・在籍時期・現在の勤務先は出さない。** 記事本文はこの例外の対象外で、従来どおり丸める
- **X診断7本は、セッションの判断で内容を変更しない**（見つけたら指摘のみ。直すかどうかはCocoが決める）
  - **禁止しているのは「セッションが自分の判断で書き換えること」**（2026-08-31 明確化）。旧文は「いかなる場合も変更しない」だったが、**Cocoが回とファイルを名指しして直せと言った場合まで禁じる読み方ができた**。実際 2026-08-31 に「X診断も軽くする。9/1週からやる」という指示と正面衝突している
  - **Cocoの名指しの指示があるときだけ直してよい。** そのとき作業報告に**どの回の何を変えたか**を一覧で出す（`rules/shindan.md`「X診断を直してよい条件」）
  - **他のセッションからの指摘・ツールの検出・自分の気づきは、すべて「指摘のみ」のまま。** ここは変わらない

### 使える数字は4つだけ
**ホスピタリティ20年／延べ5万人を超える接客／約40名のチーム運営／月商500万→1,000万。** これ以外は作らない。

**この節が全媒体の正典。`rules/*.md` にも `reference/*.json` にも複製を置かない**（2026-08-30：posts・check・note の3箇所に複製があり、note側だけ「3つ」になっていた。2026-08-31：`brand_profile.json` と `coco_voice_source.json` にも複製があり、両方とも「3つ」かつ「延べ約5万人」で止まっていた。**複製は必ず古くなる。参照だけ置く**）。

- ⚠️ **「延べ**約**5万人」は誤り。正しくは「延べ5万人を**超える**」**（2026-08-31 Coco訂正・恒久ルール）。「約」と「超える」は逆の意味で、旧表記は実数を下に丸めていた。**「約5万人超」のように「約」と「超」を同時に使わない**（意味が壊れる）。表記は「**延べ5万人を超える**」で固定し、「5万人超」「5万人以上」「五万人」も使わない

- 月商500万→1,000万は**臨機応変枠**。店舗・法人向けの回で効果的なときだけ。常用しない
- **どの数字も1〜2行目には置かない**（権威先出しは実測で負け型）
- 週内で使うのは**3本まで**

### 文体
一人称は「**わたし**」（既存記事タイトルの引用だけ原表記のまま）。恋愛対象は「お相手さま」「大切な人」——**「恋人」は使わない**。「んです」禁止。AI定型（「〜と言えるでしょう」「いかがでしょうか」「大切なのは〜すること」「寄り添う」）排除。語尾は **ね／よ** の口語基調。**「だ」は使うが連発しない**（2026-08-27 Coco決定・恒久ルール／旧「ね／よ／だ」から変更）——**1投稿に「だ。」は1回まで**。代わりに **「〜こと。」と体言止め**を使う。説教調禁止。

---

## 読者の状態（すべての判断の起点）

相談するほどじゃない。でも頭がぐるぐるする。悩みと呼んでいいか自分でも分からないから、検索もしないし人にも言わない。だから開くのは検索窓ではなくSNS。しかも夜。

- **ぐるぐるが止まらない理由は、答えが出ないからではない。決めていいと誰にも言われていないから止まらない。悩みが重いのではなく、未決が重い**
- だからぐるぐるは大きな決断では止まらない。**小さくても「決まった」瞬間に止まる**
- 一件片づいても終わらない。翌週には別の相手で同じ形が起きる。だから「解決する場所」ではなく**「繰り返し来る場所」**として設計する
- **「抜け出す」の定義**：悩みが起きなくなることではない。同じ形の悩みが来た時、ぐるぐるせずに処理できる状態になること

---

## 七つの原理

`reference/coco_methodology.json` が正典。**これは処方集ではなく、処方を作るための原理。** 素材（`coco_voice_source.json` / `episodes_soshiki.json`）にある場面は素材の言葉を使い、**素材にない場面のみ、この原理から組み立てる**。

| # | 原理 | 条件・禁止 |
|---|------|-----------|
| 01 | **相手や環境を変えない。自分の側で今日決められることだけを渡す** | 「相手の同意も、上司の許可も、組織の変化も要らない」を満たさないものは処方にしない |
| 02 | **悩みを消さない。扱えるようにする** | 禁止＝気にしないようにする／忘れる／ポジティブに考える |
| 03 | **事実と感情を分ける。記録に残すのは事実だけ** | 日付・数字・事実だけ残す。**組織側（X）の骨格だが、週内で偏らせない** |
| 04 | **決断を相手に返す。詰めない、選ばせる** | 二択で出して選ばせる |
| 05 | **二択のどちらも選ばず、三つ目の軸を置く** | 前提を組み替えて、測る対象そのものを変える |
| 06 | **悩みの意味を反転して定義し直す** | 読者が自分を責めている地点をそのまま裏返す。**ここが〈ひとこと〉になることが多い** |
| 07 | **現実を先に置いてから、その中での動き方を出す** | 「〜が現実」を先に認めてから、動ける範囲を渡す |

**書いた処方が原理01〜07のどれにも当てはまらないなら、それはCocoの処方ではない。** 書き直すか、Cocoに返す。

### 三つの前提（原理の下のレイヤー）
差別化は新原則を出すことではなく、**同じ原則を違う前提の上に置き直すこと＋実行率**。

| # | 前提 | 一般論との差分 | 主軸 |
|---|---|---|---|
| ① | **他人の本心は不可知** | 確かめずに仮置きして動く | **Threads** |
| ② | **感情を落として事実だけ渡す** | 感情を観察可能な事実に翻訳してから出す | **X** |
| ③ | **動かないことを手として持つ** | 静観を能動的な選択肢として保持 | **X** |

一般論を「間違い」と叩かない（「効かない場面がある」として置く）／前提の話は必ず具体的場面に降ろして閉じる／**三つの前提の出どころを説明する形にしない**。
**適用範囲**：前提の提示までで止めるのは**会員帯とfunnel返信だけ**。**拡散帯は結論まで無料で渡す。**

### 処方の作り方（素材にない場面）
1. 相談を、読者が持ち込んだ**二択に還元**する
2. **原理05**で三つ目の軸を探す。測る対象を変える
3. **原理06**が使えるなら、悩みの意味を反転して定義し直す（＝〈ひとこと〉）
4. 処方を**原理01の条件**で書く。今日できること一つだけ。**二つ以上出さない**
5. **原理04**で、最後の判断を読者に返す
6. **帯を確認する**（下記）

### 帯ごとの処方の渡し方
| 帯 | 処方の扱い | 締め |
|----|-----------|------|
| **拡散帯（27本＝Threads17＋X10）** | 処方は渡す。**主語なし（ゼロ主語）が基本**。結果は両方提示（来た／来なかった）。**今日か明日できる具体アクションを1つだけ置く**（2つ以上は置かない）。**切り捨て＝何をやめる・外す・線を引くかを明示する** | **名前をつける一行**（150〜200字） |
| **導線帯（funnel）** | 本文は拡散帯と同じ勝ち型。**note誘導は「この続きがないと失敗する」形にする**——線引きを間違えたときに何が起きるかを示してから渡す。「ここに残していく」で終わらせない。本格的な導線は self_replies へ | 名前をつける一行 → 続きが要る理由 → note案内 → 💍 |
| **会員帯** | 場面と問いまで見せて、**処方は渡さない** | 読者を止めきらない |

- **35投稿の帯の内訳＝拡散帯27本（Threads17＋X10）＋導線帯8本（Threads4＋X4）**（2026-08-31 明記）。**旧表記「拡散帯（17本）」はThreadsだけの数**で、全媒体の表なのにXの拡散帯10本がどこにも属していなかった。**会員帯は35投稿には無い**（noteの有料側と funnel返信の設計に効く帯）
- **字数の「150〜200字」はThreadsの数字。** Xの字数は `rules/posts.md`「X投稿（14本）の書き方ルール」が正典

### 無料と有料の役割
- **投稿（無料）＝伴走**：一件ごとの処方。一回ごとに消費される
- **note（有料）＝出口**：**すべての処方が同じ原理から出ていることを言語化するのが本体**
- **会員の意味**：「悩みを解決する対価」ではなく**「ぐるぐるする時に開く場所を持っておく対価」**
- **原理×場面**：原理は7つ、場面は無限。**毎週増えるのは場面、変わらないのは原理**

### 深さの層（2026-08-31 Coco決定・恒久ルール）

**X・Threadsは、流し見でも軽くうなずけてスッキリできるものでいい。深く知りたい人は診断をやるし、noteを読む。**

| 層 | 読者の状態 | 一本の仕事 |
|---|---|---|
| **X・Threads（35投稿・診断）** | **流し見。指が止まるかどうかだけ** | 軽くうなずける。スッキリして終わる |
| **間合い診断（8問）** | 自分から一段入ってきた | 型の名前を渡す |
| **note（有料）** | 深く知りたいと決めた | 原理まで言語化する |

- **これは「薄くしろ」ではない。「積むな」。** 拡散帯は結論まで無料で渡す（上の「適用範囲」）——**渡す一手は減らさず、その上に思想・実績・二本目の言い切りを積み上げない**
- **読者が深さを選ぶ。こちらが先に渡さない。** 深さを投稿に積むと、①流し見の人は重くて離れ ②深く知りたい人は投稿で足りてしまって診断もnoteも開かない。**両方外す**
- **診断の日替わり4択は、いちばん軽い層。** 仕事は3つだけ——**①スクロールを止める ②一文字コメントさせる ③8問へ送る**。思想を伝えることは、この投稿の仕事ではない
- **実例（2026-08-31）**：8/25週 Threads診断 08-31 が、1つの投稿に「実績5万人／4行解説／言い切り3本／北極星／接続」を積んで、**Coco本人が「くどい・主張が強い」と判断した**。作り方は `rules/shindan.md`「積まない」

---

## 収益モデル（すべての判断の前提）

**柱は noteメンバーシップ。単品2プラン 月額1,500円＋セット『間合いの整え方』月額2,500円。X収益分配は柱にしない。**

**読者への約束は「自分らしく、愛される。」**（2026-08-30 Coco決定・恒久ルール）。**約束と軸と売り物は層が違う。この3層はここが正典で、`rules/*.md` に複製を置かない**——全媒体のCTA・販売ページ・診断の出口が同じ言葉を使う必要があるため、媒体別ファイルに置くと必ず片方が古くなる。

| 層 | 中身 | どこに出るか |
|---|---|---|
| **約束**（読者に何が起きるか） | **自分らしく、愛される。** | バナー・プロフィール・販売ページ |
| **軸**（何で解くか） | **間合い**（温度 × 距離。詳細は `rules/type.md`） | 診断・型・商品名・投稿の骨格 |
| 　└ 同（軸の別名＝商品名） | **『関係の温度と距離を整える技術』** | 各媒体のプロフィール・`brand_profile.json`。**独立した層ではない** |
| **売り物**（読者に出す言葉） | **決めていい、と言ってくれる場所。** | 販売ページ・CTA・診断の出口・入会案内 |
| 　└ 同（書き手の内側の軸） | **在り方（Being）** | **読者に出さない。** `rules/note.md` の在り方署名が守るのはこちら |

- **3つ並べて1本になる**——「**間合いを整えると、自分らしいまま愛される**」
- **商品名は「軸の別名」であって、5つ目の層ではない（2026-08-31 Coco決定・恒久ルール）。** 「温度と距離を整える」＝「間合いを整える」で、指しているものが同じ。だから**商品名を新しくしない**——プロフィール4本・OG画像・販売ページの焼き直しが連鎖するのに、指すものは1ミリも変わらない
  - ただし**判定の基準にはしない。** 商品名で合否を出すと、`rules/type.md` が診断トップの h1 を「あなたの間合いには、型がある。」へ差し替えた事実と噛み合わなくなる。**ブランド整合を判定するときに見るのは、この3層の枠内かどうか**（`rules/check.md` ⑥-1・`rules/note.md` ①-1）
  - **温度と距離は、ドメインで分けない。** 💼組織『温度の整え方』／💗恋愛『距離の整え方』は**プラン名としての通称**であって、軸の割り当てではない。**両ドメインに温度軸と距離軸の両方がある**（`rules/type.md`「軸と10型」＝ドメインごとに温度×距離で4型）。**媒体で分けるのはドメイン（組織と仕事／恋愛と関係）であって、軸ではない**
- **軸を約束に差し替えない。** 約束は結果なので、それだけでは処方が作れない。逆に軸だけでは「で、どうなるの」に答えられない
- **売り物を「決めていい、と言ってくれる場所」にする理由**（2026-08-30 Coco決定・恒久ルール）：下の「読者の状態」が**「決めていいと誰にも言われていないから止まらない」**で、原理04（決断を相手に返す）がその動作。会員の意味（ぐるぐるする時に開く場所）とも同じ形になる。そして**解決を約束していない**ので、CTAの禁止に触れない
- **「在り方（Being）」を読者向けの言葉として出さない。** 買う側は在り方を買わない。ただし**書き手が顔を外さないための軸としては維持する**（noteの在り方署名は従来どおり全記事に適用）
- **量を売り文句にしない。** 週49本・週9本・在庫130本は販売ページにもCTAにも書かない（`rules/posts.md` ルール4「必死に見えると値段が下がる」は、投稿だけでなく販売ページとnoteにも効く）。量の代わりに出すのは**「毎週、あなたの型の回が、必ず1本ある」**

| プラン | 価格 | 由来 | 目標 |
|--------|---|------|-----|
| 💼 組織『温度の整え方 —— 組織と仕事』 | 1,500円 | X（板挟み管理職・経営者） | 1,000人 |
| 💗 恋愛『距離の整え方 —— 恋愛と関係』 | 1,500円 | Threads（恋愛・距離に悩む読者） | 1,000人 |
| 🕊 両方『間合いの整え方 —— 仕事と恋愛、両方』 | **2,500円** | 診断で2つの型を名乗った読者 | — |

- **セットは値引きではなく「選ばせないこと」が商品**（詳細と、2,000円・3,000円を採らなかった理由は `rules/note.md`「セットプラン『間合いの整え方』2,500円」）
- **必要な会員数は、セットの選択率で変わる。** セット6割なら ARPU 2,100円 → 手取り300万（手数料10〜15%）に**約1,600〜1,700人**。単品だけなら約2,300人なので、**セットは目標を約700人下げる**
- **セット会員は💼と💗の両方に数える。** だから「各柱1,000人」と「合計1,600〜1,700人」は矛盾しない
- 会員転換率は濃いフォロワーの1〜3% → 各柱3〜10万フォロワーが必要。**月100万インプレはその通過点**（収益源ではない）
- 解約想定 月5% → 1,000人維持に月50人前後の新規が必要。**週49投稿ルーティンが解約補充のエンジン**
- 深堀りnoteは `note_funnel: true` の投稿（週8本）からのみ作る

### 定番プロミス（funnel投稿のCTAに統一使用）
> **メンバーシップは、毎週深堀りが増えて、過去の整え方もぜんぶ読めます。**

本文では価値を完結させ、CTAは返信に置く（押し売りにしない）。**CTAで「解決」を約束しない**——約束するのは**「ぐるぐるした時に開く場所」**であること。

### 即効と継続
| | 単位 | 役割 |
|---|---|---|
| **即効** | 1投稿＝今夜のぐるぐるが止まる | **獲得**の燃料 |
| **継続** | 来週も別の形で来る | **維持**の燃料 |

- **効果の即効は約束しない。約束するのは所要時間のほう**（読むのは五分／やることは一つ／今日決められる）
- 人が即効性のあるものを選ぶのは「早く効く」からではなく、**「早く始められる・小さく試せる・失敗しても損が小さい」**から
- **順番を入れ替えない。即効が先、継続が後**

### 実測でしか配分を変えない
`reference/weekly_metrics.json` にCocoが週1回・5指標を記入する（拡散帯の平均表示／最高表示の型／フォロワー純増／noteの週間ビュー／新規入会数）。**配分・型・用量を上書きしてよい根拠は、このログの実測のみ。** 記入がない週はスキップ可。

---

## Cocoへの質問の作法

**Cocoが「パッと見て、その場で答えられる」形にしてから聞く。** 考えさせる質問・調べさせる質問・漠然とした質問は投げない。

- **何が欲しいかを名指しする**（「実例をください」ではなく「〇〇についての実例が欲しい」まで）
- **どこに使うかを添える**（投稿ID・note名・どの位置に入るか）
- **選択肢か、一行で答えられる形にする**。二択・三択で出せるものは選択肢に
- **A案とB案を並べるだけにしない。推奨と理由を先に書く。** Cocoは可否だけ答えれば済む形にする
- **答えなくても進む場合はそう書く**（「なければ空欄のまま進めます」）。返答を待って作業を止めない
- 一度に聞くのは**3件まで**

### あいまいなときは「素材不足」と名指しし、関連投稿の全文を貼って聞く（2026-08-31 Coco決定・恒久ルール）

- **判断に迷ったら埋めない。** 素材が足りないのか、素材と違うことを書いたのかが分からない状態で、汎用の言い回しで通して先に進めない。**「素材不足です」と名指しして聞く**
- **関連する投稿・記事の全文をそのまま貼る。** 投稿IDと要約だけでは、Cocoの側で食い違いが見えない。**食い違いは、本文を読んで初めて見える。** 順番は 質問 → 全文 → 答えなくても進むか
- **粒度の見本を先に書かない。** 見本を添えると、Cocoが本文を読む前にこちらの創作を読むことになり、答えがその形に引っぱられる。見本を出すのは、Cocoから「例を出して」と言われたときだけ
- **実例（2026-08-31）**：th_09（関係の棚卸し）に在庫がゼロだと分かった時点で、投稿IDと要約だけで質問し、粒度の見本を自分で書いて添えた。素材を受け取ったあとで「名前を書いた時点で会いたくなった相手もいた」が創作だと判明した。**全文を貼っていれば、質問の時点でCocoが見つけられた**

> 【実体験が欲しい・素材不足】th_09（9/3・関係の棚卸し）の5段落目に使います。
> **棚卸ししたあと、実際に何が変わりましたか。** 一行で足ります。
> 全文はこちら——
> （投稿の本文をそのまま全文貼る）
> なければ、その一行を落として公開できます。

---

## note作成の土台

**noteは投稿を土台にする。1本のnoteにX由来とThreads由来を混ぜない**（💼組織と💗恋愛は別プラン）。

**これ以外の土台ルール——起点のファイル・公開日の合わせ方・ブランチの取り直し——は `rules/note.md`「note作成の土台ルール」が正典。ここに複製を置かない**（2026-09-02：4行とも向こうと同じことを書いていて、片方だけ直せば食い違う形だった）。
