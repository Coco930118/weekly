# 申し送り（2026-09-01）

**寿命つきの文書。** 担当セッションが消化したら、その項目を削除する。**ここにルールを書かない**——ルールの正典は `CLAUDE.md` と `rules/` だけ。ここにあるのは「何が起きているか・どこを直すか・誰が」だけ。

---

## note セッションへ｜`note_2026-09-02_morning` が、同じ記事の中で順番を二回言っている

**出どころ**：35投稿セッション（2026-09-01・E434の反映作業中に発見）。**機械では検出できない**（`promise_map` は一致していて、md と html も同期しているので `note_check --week` は 0件のまま）。

### 何が起きているか

同じ記事の**無料エリアの中で、200字ほど離れて、逆の順番が書かれている**。

| 位置 | 文 | 順番 |
|---|---|---|
| 「言えなかった夜を、選んだ夜にする」節 | **わたしは、夜のうちに自分あてに打っておく。**（…）翌日、自分の打った文章を見返す。 | 夜に打つ → 翌日に判定 ✅ |
| 「## 翌朝の一行で何が変わるか」節の冒頭 | 「今夜言えなかったのは〇〇だった」——**翌朝に自分あてに一行書く。** | 翌朝に書く ❌ |

- 素材の正典は **E434**（`reference/episodes_soshiki.json`）。**夜に打つ＝処理／翌日に見返して送るか保留かを決める＝判定**。前者が正しい
- **両方とも無料エリアにある**（境界は「ここから先は、メンバーシップの中で読めます」より前）。**新規読者が最初に読む側で食い違っている**
- 記事タイトルと見出しが「翌朝の一行」なので、**古い側のほうが記事の顔になっている**。直しは一文だけでは終わらない可能性がある（見出しをどうするかの判断が要る）

### 直すところ

`notes/note_2026-09-02_morning.json`

1. `content_markdown` の「## 翌朝の一行で何が変わるか」節の冒頭 `「今夜言えなかったのは〇〇だった」——翌朝に自分あてに一行書く。` を、夜に打つ順番に合わせる
2. `content_html` にも同じ文がある。**両方直して同期を保つ**（`note_check` は md/html 同期を見る）
3. 見出し「翌朝の一行で何が変わるか」と、記事タイトル「💗本音が言えた夜と言えなかった夜の差は、翌朝の一行だ」を残すか変えるかは**note セッションの判断**。`promise_map` のキー（`言えなかった夜の選び方` / `翌朝の伝え直し方`）は投稿側の約束なので**動かさない**

### 投稿側の状態（済み）

`th_06`（9/2 19:00・funnel）は 2026-09-01 に E434 の順番へ直してある。

- `今夜ひとつ、言えなかった一言を自分あてに打っておいて、送るかどうかは翌朝に決めてみる？`
- `翌朝に送った日も、保留にして考えるのをやめた日もあった。`
- 締め：`言えなかった夜は、翌朝の読み返しで、決め直した夜になる。`

**投稿と、noteの本体は一致している。ずれているのは上の一文だけ。**

### 経緯（同じ事故を繰り返さないため）

2026-08-30、35投稿セッションが「noteが伝え直しを否定している」と誤読して `th_06` の約束を書き換え、note セッションがそれに合わせて note の「## 翌朝、どこまで言い直すか」節を削除した。**投稿とnoteの両方が誤った側に揃い、機械チェックは通ったまま**だった。8/31 に Coco の指摘で両方とも戻っている（節は復元済み）。

**約束と回収の突き合わせは、否定文の一行だけを見て判断しない。回収先の見出しの中身まで読む。**

---

## 35投稿セッション｜Coco待ち 1件

`th_02`（9/1 10:00・**配信済み**）のどこを直すか。三択で Coco に出してある（①E429後半を入れる ②締めの「目盛り」を外す ③別の場所）。**推奨は②。** ①は E429 の `usage_note` ③「後半は処方の副作用側。『向かない場面』に置く」に触れるため、素材の扱いを一つ動かす判断が要る。
