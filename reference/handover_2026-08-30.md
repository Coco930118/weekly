# 申し送り（2026-08-30 恒久ルール整理セッション）

**寿命つきの文書。** 全セッションが一度読んで、担当ぶんを消化したら削除する。**ここにルールを書かない**——ルールの正典は `CLAUDE.md` と `rules/` だけ。ここにあるのは「**何が変わったか・どこを読み直すか・誰が何をするか**」だけ。

対象コミット：`a70d1b8` → `8270aad`（25コミット・すべて `main` にマージ済み）。動いたのは `CLAUDE.md` / `rules/` / `reference/` / `tools/` のみで、**`posts/` `notes/` `index.html` `assets/` は1バイトも触っていない**（公開サイトの中身は不変）。

---

## 0. 全セッションが読み直すもの

**`CLAUDE.md` は全文。** 土台が4つ動いている。

1. **読者への約束「自分らしく、愛される。」を追加。** 約束・軸・売り物は層が違う——**約束＝自分らしく愛される／軸＝間合い（温度×距離）／売り物＝在り方**。3つ並べて1本になる：「間合いを整えると、自分らしいまま愛される」。**軸を約束に差し替えない**（約束だけでは処方が作れない）
2. **使える数字4つは `CLAUDE.md` が唯一の正典。** `rules/*.md` に複製を置かない（posts・check・note の3箇所にあり、note側だけ「3つ」に化けていた）
3. **特定回避に例外を1つ追加。** 販売ページとプロフィールでは**業種の併記だけ出してよい**（条件4つ＝店舗名・地域・在籍時期・現在の勤務先は出さない）。**記事本文は対象外**
4. **12:00グローアップの目視は「8つ」**（`rules/check.md`「目で見る8つ」）。旧版は5つのままだった

**持ち場が1つ増えた**：診断サイト反映（`Coco930118/shindan`）。指示文の正本は `reference/runbook_shindan.md`。

---

## 1. 35投稿セッション

**読み直す**：`rules/posts.md`（特に「返信と引用の書き方」「拡散帯の字数」「締めと語尾」）

- **【新設】返信と引用の書き方。** X＝引用／Threads＝返信を**9/1週から随時**開始（Coco実施）。**週49本の予約投稿は減らさない——上乗せの層**。引用は自分のフィードにも出るので**Xの投稿ルールがそのまま効く**が、機械では数えられないので週の用量は目視で頭に入れる
- **funnelの字数を拡散帯と統一。** 150〜200が目安・上限230・**下限130**（旧「100〜150字」は機械の下限と食い違っていて、ルールどおり書くと弾かれる状態だった）
- **型の表に締めのルールを書き足さない。** 締めの正典は「締めと語尾の作り方」1箇所
- **9/8週の生成で効かせる**（`reference/todo.md`）：整った選択肢が同じ型に3本以上寄らない／X判定先出しの構文を週3本以内／原理06をXで4本以下

---

## 2. noteセッション

**読み直す**：`rules/note.md` 全文

- **【一度きり】既存note全130本の書き直し**（約15週・週9本）。**順番は note.com の週間ビュー降順**（上位20本＝全ビューの56%）。期間中は新規funnel noteを作らない
  - **「新しいルールは過去記事に遡及しない」の原則は変えていない。** これは例外で、理由は前提が実態で崩れたから——会員0で、読まれているのは note内検索から来た新規。既存記事は「会員向けの資産」ではなく**「新規の入口」**として働いている
  - **上位9本を終えた時点で1回測る**（`todo.md`）。170→166 で頭打ちが出ているので、動かなければ残り110本の順番はCocoが判断する
- **メンバーシップの構造が確定。** プランは2つのまま、**型は「章」＝マガジンで作る**。記事の行き先は①どちらかの有料プラン ②無料マガジン の**どちらか一方**。有料記事は型マガジンにも入れる
- **全noteに型を1つ割り当てる**（`plan_type`）。**中央「保つ人」は割り当てない**
- **無料まとめの深さの上限＝X・Threadsに載せる深さ。** 深堀りは全部有料側
- **`note_check.py` の「渡す／渡し」は参考カウントへ移った**（要修正ではない）。note では「仕事を人に渡す＝委譲」という実際の行為で使われ、130本中11本・延べ128箇所ある。**比喩かどうかは目視で決める。35投稿・診断は要修正のまま**

---

## 3. 診断セッション（weekly側・`posts/shindan_*.json`）

**読み直す**：`rules/shindan.md`（特に「診断サイトの結果ページに渡すもの」）と**新設の `rules/type.md`**

- **【新設】`rules/type.md` が間合い診断（8問の型診断）の正典。** 日替わり4択（`rules/shindan.md`）とは**別物なので混ぜない**
- **【JSONの形が変わった】`axis_map` が必須項目。** A〜Dそれぞれの `temp`／`dist`（高／低）。**選択肢を型に割り当てるのは言葉の判断なので weekly が持つ**——無いとサイト側が結果ページを組めずに止まる
- **日替わり4択に中央（保つ人）を割り当てない。** 中央は「4つのどれでもない」で1問では成立せず、CTAも出さない型なので行き先がない。**中央が出るのは8問の合計スコアだけ**
- **4象限に割れないときは、まず軸の読み方を疑う。** ずれ方は2つ——**距離は「自分の側か相手の側か」ではなく「相手をどれだけ入れるか」**／**温度は「口に出すか内側か」ではなく「感情をどれだけ出すか」**。それでも割れないとき、**X診断は選択肢を変更できない**（絶対禁止）ので、その回だけ8問診断から外して報告する
- **`tools/shindan_check.py` のバグを直した。** 設問の型判定が固定CTAと本文側の語に乗っ取られ、8/25週は7本中3本が誤判定だった。「素の行動型は週2本まで」が素通りしていた
- **【急ぎ・未了】TYPES本文の正典化。** `rules/type.md` にあるのは型名と「欠けているもの」だけで、`tag`/`punch`/`ans`/`cta`/`bridge` の原文は正典に無い（サイトが持っている）。**実装から書き起こして weekly を正にする**。あわせて「あなた」14箇所を落とす／組織側の「欠けているもの」3つを実装から書き起こす（照合は恋愛4/4・**組織1/4**）／中央の「もう片方のドメインへ送る」文言を作る／Q8を固定して正典化

---

## 4. 診断サイトセッション（`Coco930118/shindan`）

**読み直す**：`reference/runbook_shindan.md`（指示文4本の正本）

- **前提が変わった。** アカウント統合で **weekly を読める**。**正典を直接読む**——指示文にルールを書き写さない。**読むのはよい／weekly を書き換えるのはしない**
- **`main` を読む。** 正典は `main` にマージ済み（`8270aad`）
- **リポジトリの見分け方**：ルート直下に `CLAUDE.md` `rules/` `tools/` `protocol_v7.3.md` の4つがあるのが本物。5点だけなら古いフォーク
- **結果ページの3つは、どれも weekly に原文がある。**DAY_ANSWER＝`comment`／`reply_1` から切り出す・TYPES＝`rules/type.md`・配点＝`axis_map`
- **KVのキーは指示文に書かない**（環境変数から。**読み取り専用トークンで足りる**）
- **日替わりページのOGにキャラは出さない**（キャラが主役なのは `/type` の結果OG）
- **サイト側 `CLAUDE.md` から言葉のルールを落とす**（未了・`todo.md`）。§③「③で処方を渡さない」は旧版で、weekly の「拡散帯は結論まで無料で渡す」と衝突している。**これが二重持ちの本体**——指示文から外しても、リポジトリ側の `CLAUDE.md` に残っていたら同じこと

---

## 5. Wチェックセッション

- **計測ドクトリンが2つ変わった。**①**Threadsの表示は約24時間でほぼ確定する**（8/25の3本が day1 26/64/42 → day5 27/65/43）。「経過日数に比例」は成立しない ②**主指標を平均から中央値へ**（8/18週 平均133／中央値61 → 8/25週 平均29.7／中央値22。1本の跳ねで平均が壊れる）
- **90日の通過点5つ**（12/1）と**母数のレバー3つ**が `reference/todo.md` に入った。**①〜⑤は月1回（月初）だけ見る**——週次で見ると母数が小さすぎてノイズしか読めない
- **出口（結果シェア）の要修正4件。** 最大は**型別OGがシェアに載っていない**こと（98枚あるのに共通1枚が流れている）

---

## 6. このセッションが間違えたこと（次に同じ穴を踏まないために）

**7件。全部、正典と実態のズレが原因。**

1. **持ち場を外れて `notes/` を編集した**（画像プロンプト6本）。復元済み・push なし。**恒久ルールの点検セッションは、ルールとreferenceとtoolsしか触らない**
2. **「main を読め」と書いたが、正典を main にマージしていなかった。** 診断サイトのセッションが2回止まった。**他リポジトリに読ませる前に、まずマージする**
3. **OG「キャラ主役で統一」は逆だった**（キャラが主役なのは `/type` の結果OG）
4. **「TYPESの正典は `rules/type.md`」が2回とも不正確だった。** 実際にあるのは型名と「欠けているもの」だけ
5. **「同じ象限に2つ入る回は選択肢のほうを直す」は、X診断では不可能な道だった**（絶対禁止に「いかなる場合も変更しない」がある）
6. **レバーAを「拡散帯17本から5本を振り替える」と設計した。** 予約投稿と返信・引用は**別の層**で、置き換える理由がなかった
7. **「恋愛に寄せる」「Threads診断を止める」を提案して、どちらも撤回した。** 前者は note上位20本の72%が組織という実測に反していた。後者は根拠が**1週間の相関だけ**だった

### 3つの教訓

- **語を置換しても概念は消えない。**「ブレンド型」を「保つ人」に直したが、日本語訳の**「配合で生きてきた人」が共有文に残っていた**。次の全週監査では、語ではなく**概念**で洗う
- **正典が実装の後追いになっていた。** TYPESも `axis_map` も、実装のほうが先に持っていた。**言葉が正典で実装が従う、という建て付けが、実態では逆転していた**
- **二重持ちは指示文だけに潜むのではない。** 別リポジトリの `CLAUDE.md` にも同じ形で入る

---

## 7. 今週の期限

| いつ | 何を | 誰が |
|---|---|---|
| **今日** | 9/1週 X診断 09-07 の `axis_map`（残り1本） | 診断セッション |
| **今日** | `data-w8.js` の完成とデプロイ | 診断サイトセッション |
| **9/1** | 間合い診断の公開（noindex解除・TOP導線） | Coco |
| **9/3まで** | 水15:00のルーティン登録（**リポジトリを2つ**：shindan＝書く／weekly＝読む） | Coco |
| **土曜** | note書き直し 第1バッチ（上位9本） | noteセッション |
| 公開後 | TYPES正典化・Q8固定・「あなた」14箇所・中央の文言・OG画像 | 診断セッション／Coco |
