# 指示文｜9/22週のnote ── `promise_map` を、いまの返信2に当て直す（noteセッション）

> **一度きりの指示文。済んだら消す。**
> **ルールの正典は `rules/note.md`「funnel note は `promise_map` を持つ」。この指示文にルールを書きません。必ず読んでから始めてください。**
> これは 35投稿セッションからの申し送りです。**投稿側はもう動きません。**

## なぜ当て直しが要るのか

2026-09-21〜22、**9/22週の funnel 投稿8本の返信2を直しました**（X＝x_02・x_03・x_05・x_08／Threads＝th_03・th_05・th_09・th_11）。

直したのは3つ。

- **定番プロミスの1文**（8本とも）。正典と4か所ちがっていた
- **x_03 返信2**の禁止語「どう戻したか」→「どう決め直したか」
- **x_05 返信2**の禁止語「どう戻すのか」→「どう収めるのか」

**`promise_map` のキーは返信2の literal な一片**なので、返信2が動くとキーが当たらなくなります。

## 実測（2026-09-22）── 材料です。判定はそちらで

再現するコマンドは下の「確かめ方」。

| note | 投稿 | 当たらないキー | いつから |
|---|---|---|---|
| `note_2026-09-23_no-answer-given` | x_03 | **1件** | **今回の修正で** |
| `note_2026-09-23_waiting-to-absorbed` | th_05 | **1件** | **今回の修正で** |
| `note_2026-09-22_notice-then-choose` | th_03 | 2件 | **前から** |
| `note_2026-09-24_one-line-not-answer` | x_05 | 2件 | **前から** |
| `note_2026-09-25_soft-but-clear` | x_08 | 2件 | **前から** |

**当たっている3本**：`fact-not-person`（x_02）／`put-it-first`（th_09）／`no-permission-needed`（th_11）。**触らないでください。**

⚠️ **今回の修正で壊れたのは2件だけです。** 残り6件は**前から当たっていません**。**同じ作業で直せますが、別の原因なので、報告では分けてください。**

### 当たらないキーの実物

```
no-answer-given（x_03）
  「唯一無二と言われた朝、最初の一行をどう決めたか。外れた日にどう戻したか。答えを待っていた時期」

waiting-to-absorbed（th_05）
  「会えない時間を「待ち時間」にしていた頃から、自分の時間に戻していくまで。好きな気持ちを
   消さずに、関係の温度と距離をどう整えていったのか」

notice-then-choose（th_03）
  「気づいたあ」
  「反応する前に何を見るか。受け身で流すのではなく、感覚を閉じずに行動だけ選び直す整え方」

one-line-not-answer（x_05）
  「高額の判断を任せるとき、絶対NGの一本をどこに引くか」
  「外れたときに戻す手順」

soft-but-clear（x_08）
  「配慮を残したまま、読み替えの余地だけを外す。その言い換え方」
  「議題の置き方」
```

## ⚠️ 先に決めることが1つあります

`rules/note.md` は「**書けない片が出たら、noteに節を足すか、投稿の返信2を書き直すかを選ぶ。どちらにするかは『読者にとってどちらが正しいか』で決める。書き直しやすいほうで決めない**」と言っています。

**今回は、投稿の返信2は Coco が確定させたものです**（2026-09-21〜22 の承認ぶん）。**返信2は動かさない前提で進めてください。** 変えたくなったら、直さずにCocoへ質問を上げる。

## 手順

1. **`rules/note.md`「funnel note は `promise_map` を持つ」を読む。** キーの作り方（`self_replies[-1]` を `と、` で割る）と値（回収した `## 見出し`）はそこが正典
2. **5本を1本ずつ、いまの返信2からキーを作り直す**
3. **値（`## 見出し`）は、実在する見出しを指しているかを確かめる。** 指していないなら、**見出しを足すのではなく、まず本文のどこが回収しているかを探す**
4. ⚠️ **`promise_map` は2件のまま。増やさない**（`rules/note.md`「約束の2見出しは1文字も触らない。`promise_map` も増やさない」）。`waiting-to-absorbed` と `no-permission-needed` は1件なので、そこは現状のまま
5. **`content_markdown` を触ったら `content_html` も作り直す**（`note_check.py` の `md2html`）
6. **意味が本当に回収できているかは目視**（`rules/note.md`）。キーが機械的に当たっただけで終わりにしない

## 確かめ方

```bash
python3 tools/note_check.py --week 2026-09-22
```

**走る前と走ったあとの件数を、両方出してください。**

キーの一致だけを見るなら、これで足ります。

```bash
python3 - <<'EOF'
import json,glob
posts={p['id']:p for p in json.load(open('posts/week_2026_09_22_2026_09_28.json'))['posts']}
for f in sorted(glob.glob('notes/note_2026-09-2*.json')):
    d=json.load(open(f)); ft=d.get('funnel_targets') or []; pm=d.get('promise_map') or {}
    if not ft or not pm: continue
    r2=(posts[ft[0]].get('self_replies') or ['',''])[1]
    bad=[k for k in pm if k not in r2]
    print(('×' if bad else '○'), f.split('/')[-1][:-5], '←', ft[0], f'当たらない{len(bad)}件')
    for k in bad: print('     ', k)
EOF
```

## 出力

- **1本ずつ「note名 ← 投稿ID → before のキー → after のキー → 値（見出し）が実在するか」**
- **「今回の修正で壊れた2件」と「前から当たっていない6件」を分けて出す**
- **当たっている3本は「触っていない」と明記する**（消したように見せない）
- 走る前と走ったあとの `note_check` の件数
- 見出しを足した本があれば、どの見出しをどこに足したかを1行で

**要修正は提案のみ。Cocoの承認を得てからJSONに入れる**（`CLAUDE.md`「修正が必要なところは、提案だけ」）。

## 触らないもの

- **投稿側**（`posts/week_2026_09_22_2026_09_28.json`）。**返信2はCocoが確定させたもの。1バイトも変えない**
- **当たっている3本**（`fact-not-person`・`put-it-first`・`no-permission-needed`）
- **9/22週以外のnote・診断・35投稿**
- `rules/` と `CLAUDE.md`（**この件でルールは1行も足さない**）

## 申し送り（この件の再発について）

**投稿の返信2を直すと、`promise_map` は必ず置き去りになります。** 今回は Final Editor の編集を戻したことで顕在化しましたが、**返信2を触るたびに起こります。**

**「返信2を直したら、その回のnoteの `promise_map` を当て直す」** ── この一行をどこに置くかは Cocoの判断です（`CLAUDE.md`「禁止ルールは、足す前に『機械か、生成か』を決める」。**`note_check.py --week` がすでに見ている**ので、運用の順番の問題です）。

⚠️ **同じ形が X短文でも起きています**（`reference/shijibun_2026-09-20_xshort_0922.md` の申し送り）。**本文が動くと、それを参照している別ファイルが置き去りになる** ── 同じ一つの問題です。
