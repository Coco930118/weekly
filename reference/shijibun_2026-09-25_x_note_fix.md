# 修正依頼｜Xと、Xに紐づくnote（2026-09-25 05:00 以降に投稿するもの）

> **一度きりの依頼。済んだら消す。**
> **持ち場が2つにまたがります。X＝35投稿セッション／note＝noteセッション。**
> **ここに書くのは「何を・どのファイルに・どの順で」だけです。条文は下の正典を読んでください。**

## 線

**2026-09-25 05:00 以降に投稿するもの**（Coco指示）。**作った日ではなく、配信する日で切ります。**

## 対象（実測 2026-09-25）

| | 対象 |
|---|---|
| **X・9/22週** | **x_07〜x_14（8本）** |
| **X・9/29週** | **x_01〜x_14（14本）** |
| **note（X由来）** | **`notes/note_2026-09-25_soft-but-clear.json` の1本だけ** |

- **Threads・診断は対象外。** この依頼では触らない
- 9/25 公開の他の2本は Threads 由来なので外れる
- ⚠️ **x_07 は 9/25 08:00 に配信済み。** リポジトリを直しても、出たものは変わらない

## やること

### ① 着地（X 22本・note 1本）

**「相手をどう動かすか」ではなく「自分がどこで決めるか」に置く。**
**正典は `rules/posts.md`「Xの声＝凛とした軍師」。ここに条件を書き写しません。**

- **9/22週 x_08〜x_14 は 2026-09-25 に読み直し済みで、書き直し0本**（着地はすでに自分側）。**x_07 と 9/29週14本は、まだ読み直していません**
- 難しい上司や組織を**「敵」として扱う構図**になっている回が、外れている回です

### ② 禁止ワード（X）

**9/29週に3件残っています**（Coco：全部直す）。

```
python3 tools/full_check.py posts/week_2026_09_29_2026_10_05.json
```

| | |
|---|---|
| x_06 返信1 | 期限をつけずに**置いてある**仕事は、どれ？ |
| x_13 本文 | 先に事実が**置かれた**面談は、姿勢の話までたどり着く |
| x_14 返信1 | 装置は、褒め言葉の形で**置かれる** |

- **動作の「置く」は対象外。** 受け身・存在の形だけ（正典は `rules/posts.md`「抽象語を使わない」）
- x_13 は**直前が「置くのは事実だけ」で、動作と状態を対にして書いている形**です。**片方だけ直すと対が壊れます**
- **9/22週のXは 2026-09-25 に直し済み**（「戻す」6箇所）。**再確認だけ**

### ③ note（`note_2026-09-25_soft-but-clear.json`・1本）

```
python3 tools/note_check.py notes/note_2026-09-25_soft-but-clear.json
```

**⚠️ promise_map が、投稿と噛み合わなくなっています。**

**この note の `funnel_targets` は `x_08`。その x_08 を 2026-09-25 に生成版へ戻したので、返信2の約束が変わりました。** いまの `promise_map` は前の版の約束を持ったままです。

- **返信2から約束を取り直して、`promise_map` を作り直す**（手順は `rules/note.md`。`note_check.py` の突き合わせが通ればOK）
- **禁止ワードも2件出ています**（「残し」「置いてあ」）。機械の出力を見てください
- **`content_markdown` を直したら `content_html` も作り直す**（`note_check.py` の `md2html`）

⚠️ **これは `reference/todo.md`「【注意・2回目】Final Editor が本文と返信を動かすと、後ろの工程が置き去りになる」の3回目です。** 直したあと、**その項目に1行足してください**（`CLAUDE.md`「ルールは v1.0 で凍結」の3回目＝恒久ルールの候補）。

## 読むファイル

1. `CLAUDE.md`（七つの原理・特に01と04）
2. **`rules/posts.md`「Xの声＝凛とした軍師」** ← 着地の正典
3. `rules/posts.md`「抽象語を使わない」 ← 禁止語の正典
4. `rules/note.md`（noteセッションのみ）
5. `rules/check.md`

## 触らないもの

- **Threads・診断・プロフィール**
- **9/22週 x_06 以前と、Threads 全部**（配信済み／この依頼の対象外）
- `rules/` と `CLAUDE.md`（**この件でルールは1行も足しません**。条文はもう入っています）
- **振り返りの一本**（別件・`reference/todo.md`「【素材あり】振り返りの一本」）

## 出力

- **X**：22本それぞれの着地の一行を並べる。直した回は before / after
- **note**：取り直した `promise_map` を before / after で出す
- `full_check.py` と `note_check.py` の件数（走る前と走ったあと）
- **直さずに残したものがあれば、理由つきで名指しする**
