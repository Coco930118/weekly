# 修正依頼｜note の約束を取り直す（noteセッション）

> **一度きりの依頼。済んだら消す。**
> **対象は1本だけ。`notes/note_2026-09-25_soft-but-clear.json`**
> **ここに書くのは「何を・どのファイルに・どの順で」だけです。条文は `rules/note.md` を読んでください。**

## ⚠️ Xの分は済みました（2026-09-25）

**この依頼は、当初 X 22本＋note 1本でした。Xの分はこのあと片づいたので、残っているのは note だけです。**

| | 状態 |
|---|---|
| X の着地（温度） | **済**。9/22週 x_07〜x_14 と 9/29週14本を読み直し、締めの一文を7本直した |
| X の禁止ワード | **済**。9/29週は `full_check` **要修正0件** |
| **note の約束** | **未。これだけ残っている** |

## やること

```
python3 tools/note_check.py notes/note_2026-09-25_soft-but-clear.json
```

**`promise_map` が、投稿と噛み合っていません。**

この note の `funnel_targets` は **`x_08`（9/22週）**。**その x_08 を 2026-09-25 に生成版へ戻したので、返信2の約束が変わりました。** いまの `promise_map` は前の版の約束を持ったままです。

1. **x_08 の返信2から約束を取り直して、`promise_map` を作り直す**（手順は `rules/note.md`。`note_check.py` の突き合わせが通ればOK）
2. **禁止ワードも2件出ています**（「残し」「置いてあ」）。機械の出力を見てください
3. **`content_markdown` を直したら `content_html` も作り直す**（`note_check.py` の `md2html`）

## 本文の温度は、直さなくてよい

**この note の締め「あなたなら、外す日を選べる。」は、判定＝凛です**（読者に判断を返していて、突き放しも慰めもない）。**基準は `rules/posts.md`「Xの声＝凛とした軍師」。ここに書き写しません。**

**x_08 の締めは funnel の導線なので、今回の温度の修正では触っていません。** 約束の取り直しと衝突しません。

## 済んだら、1行足してください

⚠️ **これは `reference/todo.md`「【注意・2回目】Final Editor が本文と返信を動かすと、後ろの工程が置き去りになる」の3回目です。** 直したあと、**その項目に1行足してください**（`CLAUDE.md`「ルールは v1.0 で凍結」の3回目＝恒久ルールの候補）。

## 触らないもの

- **X・Threads・診断・プロフィール**
- 他の note（**この1本だけ**）
- `rules/` と `CLAUDE.md`（**この件でルールは1行も足しません**）

## 出力

- 取り直した `promise_map` を before / after で出す
- `note_check.py` の件数（走る前と走ったあと）
- 直さずに残したものがあれば、理由つきで名指しする
