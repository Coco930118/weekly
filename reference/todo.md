# やること・試験中のもの

**恒久ルールにするほどではない、日付つきのもの。** 一度きりの作業、期限のある試験、順番待ちの宿題。
終わったら消す。ここが空になるのが正常な状態。

ルール本体は `CLAUDE.md` と `rules/*.md`。なぜそう決めたかは `reference/decisions.md`。

---

## 承認済みの例外（その週かぎり。恒久ルールは変えない）

- [x] **9/1週 x_10（2026-09-05 土 22:00・funnel）の土曜配置は 2026-08-29 Coco承認の例外。** 本文が「週末のたびに、この声が来る」＝Xの曜日ネタで、土曜以外に置くと前提が壊れるため。`full_check.py` は毎回この1件を要修正として出すが、直さない
  - **8/25週 x_09（2026-08-29 土）も同じ理由で承認済みの例外**（月末ネタ）
  - 注記：コミット `3cdc7fd` のメッセージは9/1週の例外を「x_09」と書いているが**誤記**。9/1週で funnel なのは x_10（x_09 は同じ土曜の06:00で funnel ではない）。承認の対象は x_10（2026-08-29 Wチェックで訂正）

### 【解決】保留していた3件（2026-08-30 Coco決定・推奨どおり）
- **①「渡す／渡し」の過検出** → `note_check.py` で参考カウントへ。実測：引用を除いて130本中11本・延べ128箇所。35投稿・診断は要修正のまま
- **②リンク先タイトルの除外** → **撤回。問題ではなかった。** `note_check.py` は `re.sub(r'「[^」]*」','')` で既に除外できている。最初の指摘は生の本文をgrepしたもので、チェッカーが見ている本文ではなかった
- **③販売ページの業種表記** → `CLAUDE.md` の特定回避に例外を明記。業種の併記は身バレ防止の目的に反さず、販売ページでは具体が信用を作る。条件は店舗名・地域・在籍時期・現在の勤務先を出さないこと

## 90日の通過点（2026-08-30 Coco登録・期限 2026-12-01）

**到達点（月100万インプレ／月収300万／会員2,000人）は変えない。**変えるのは**週の判断基準**のほう。到達点をそのまま毎週の物差しにすると、何をやっても「まだ1000分の1」で、効いた施策と効かなかった施策の区別がつかなくなる。

**現在地（2026-08-30 実測）**：X 35／Threads 40／note 6 フォロワー。拡散帯の中央値22（8/25週）。note 週166ビュー。**会員0**。月間インプレ 約7,800〜1.2万。

| | 指標 | 現在 | 12/1 | 測る場所 |
|---|---|---|---|---|
| ① | Threadsフォロワー | 40 | **500** | `weekly_metrics.json` follower_delta |
| ② | 3,000表示超えの本数 | 3週で1本 | **月3本** | 同 top_post |
| ③ | 間合い診断の完了 | 実質0 | **週20件** | KV（診断サイト側が読む） |
| ④ | noteフォロワー | 6 | **100** | note.comダッシュボード |
| ⑤ | **会員** | **0** | **10人** | note.comメンバーシップ |
| ⑥ | **拡散帯の中央値** | **22** | **60** | `weekly_metrics.json`（2026-08-30 追加） |

**⑥を足した理由（2026-08-30 Wチェック）**：②「3,000表示超えを月3本」は中央値22から見て136倍で、他の4つ（現在地の10倍前後）と粒度が違う。**返信・引用（レバーA）の効果が最初に現れるのは中央値**なので、先に動く数字を持っていないとレバーが効いているか判断できない。

**⑤が本丸。0→1は、1→1,000より難しい。** 10人入れば転換率が実測できて、そこで初めて2,000人の逆算ができる。いまの「1〜3%」は業界平均であって、Cocoの数字ではない。

**判定の作法**：①〜⑤は月1回（月初）だけ見る。週次で見ると母数が小さすぎてノイズしか読めない。

### 母数のレバー3つ（率ではなく母数を動かすもの）

- [x] **レバーA：返信（Threads）と引用（X）を随時。**予約投稿の49本は減らさない——**上乗せの層**。2026-08-30 Coco承認・X引用は開始済み。書き方は `rules/posts.md`、測り方は下の「試験中」
- [ ] **レバーB：Threads診断の形式をX診断に揃えて1週試す。**実測は 8/25週で X診断 103%→**177%**／Threads診断 18%→**11.4%**（7日中4日はコメント0）。同じ週に片方だけ悪化しているので原因は形式に絞られる。X診断の新形式（comment 1通統合・固定CTA・末尾URL）をそのまま移植する。**止めない**（母数が小さすぎて中止の根拠にならない）
- [ ] **レバーC：出口＝結果のシェア**（下の節）

---

## 出口（診断結果のシェア）2026-08-30 Coco提供のスクショから

**入口はもう十分磨いた。**実測が言っているのは、磨いても母数30人では診断アクセスは月に数件が上限だということ。**効くのは出口——1人の完了が新しい流入を生む形。**現物を見て、直すところが4つ。

- [ ] **【高】OG画像が型別になっていない。**シェアされたカードは全員共通の「いま、どこに立っているか。」（4象限が全部「?」）。**98枚用意した型別OGが、シェアに載っていない。**タイムラインでは本文は読まれず画像だけ見られるので、10人がシェアしても同じ絵が10回流れるだけで蓄積しない
  - **直し方**：シェアURLに型のクエリを付ける（例 `/kankei?t=LL`）。**OGだけ型別にして、遷移先は診断の1問目のまま。**ネタバレも離脱も起こさずに、画像だけ型別にできる
  - 実装は診断サイト側の持ち場（`Coco930118/shindan`）
- [ ] **【中】ハッシュタグが `#温度と距離`。**これは軸の名前で、誰も検索しない語。**タグの仕事は「同じ診断をやった人の結果が1か所に溜まること」**なので、通称の **`#間合い診断`** に替える（`CLAUDE.md`／`rules/type.md` で決まった通称）。タグは1本に統一する（型名を入れると分散する）
- [ ] **【中】結果ページの一行と、シェア文の一行が同じ。**「揺れないし、崩れない。扉を開ける時を、自分で決められる型。」は**褒め**で、シェアには強い（見せて恥ずかしくない）。ただし守る人の欠けているものは「閉じたまま動かしていない」＝**原理06の反転が効いていない**。10型ぜんぶ褒めで揃うと「当てられた」感が消えて、会員へ進む理由がなくなる
  - **提案：2本に分ける。**結果ページ＝見立て（痛いところ＋逃げ道）／シェア文＝見せられる形（褒め）。**言葉の持ち場なので `rules/type.md` 側で決める**
- [ ] **【低】OG画像の「4タイプ」と、正典の「10型」が食い違う。**ドメインごとに4型＋中央＝5。中央「保つ人」を数に入れるかどうかを決めて、表記を揃える
  - あわせて：シェア文に型数の数字が無い。MBTIの拡散は「16のうちの1つ」という希少性が効いている。テキスト側にも数字を入れる価値がある
- [ ] **確認だけ**：結果ページ下部のCTA（守る人 → note会員へ／保つ人 → もう片方のドメインへ）が実装されているか。スクショでは切れていて見えない

---

## 【完了】診断サイトの統計ページ（2026-08-30・診断サイト側セッションが修正）

`stats.html` の `WK_DATES` と週タブが w1〜w6 の手書きリストのままで、w7（8/25–31）が無かった。KVにデータが入っていても「今週」タブは永久に w6（8/18–24）を指す状態。**w5→w6 のときにも同じ漏れが起きていた＝再発。**

- 週の区切りを「w1＝2026-07-14 起点・7日間隔」の計算式に置き換え、日付レンジと「今週」タブを現在日から自動算出するようにした（手動リストという原因そのものを解消）
- `api/stats.js` が読む週キー（`w1`〜`w8` 固定）も同じ基準の自動計算に変更。w8・w9 が来ても手を加えずに追従する
- ローカル検証済み：w7＝08-25〜08-31、8/30 は w7 と判定
- **9/1公開後に、週の切り替わりで stats.html を手直しする必要はなくなった**

---

## 一度きりの作業

### 【大】既存note全130本の書き直し（2026-08-30 Coco決定・約15週）
週9本 × 約15週。**期間中は新規のfunnel noteを作らない**（無料まとめ1本だけ新規）。手順は `rules/note.md`「既存note全書き直し」、指示文は `reference/runbook_weekly.md` の⑦。

### 【最優先】型別の道案内8本と、最初の3本（2026-08-30 Coco承認・note持ち場）
**新規note週9本は止めない**（2026-08-30 Coco訂正。旧「期間中は新規noteを作らない」は撤回）。**型と原理は既存note全書き直しの中で全130本に付く**ので、そのために別の作業を立てない。ルールは `rules/note.md`「入会後の『最初の3本』と、型別の道案内8本」。

- [ ] **① 書き直しの中で `plan_type` と `principle_no` を付ける**（手順1・4）。2026-08-30 実測：`plan_type` は130本中**0本**、`principle_no` は**111本が null**、`platform_origin` unknown が23本
  - 割り当て：X由来→組織4型／Threads由来→恋愛4型。原理は 受け止める（06・07）／整える（02・03・05）／動く（01・04）
- [ ] **② 道案内8本を、同じセッションでまとめて作る**（型と原理が全部そろった直後がいちばん安い）。中身は目次＋一行案内だけ。**新しい処方を書かない**
  - 締めの固定一行：**「毎週、あなたの型の回が、必ず1本あります。」**
- [ ] **③ note.com側**：無料マガジンに8本を追加＋各型マガジンの先頭に固定（`reference/note_com_edit_list.md` にも載せる）
- [ ] **④ 診断サイト側へ渡す**：型ごとの道案内URL8本。結果ページのCTAを「メンバーシップ」から「その型の道案内note」に差し替える（診断サイトの持ち場）

### 【解決】note.com側の確認2点（2026-08-30 Coco確認・どちらも設計どおり）
- [x] **① 3つ目のプランは作れる。有料記事を複数プランに同時公開できる。** → セットは公開範囲のチェック2つで実現できる。記事側の組み直しは不要
- [x] **② メンバー限定記事のタイトルは、非会員にも見える。** → 型マガジンの目次がそのまま販売ページとして働く。代替案（無料まとめ＋リンクで組み直す）は不要

**未確認だった2点がどちらも設計どおりだったので、`rules/note.md` のメンバーシップ構造は1文字も変えずに実装できる。**

---

## 提案（Coco判断待ち・恒久ルールにはしていない）

### 【提案B】定番プロミスに、入会後の一行を足すか
現行「毎週深堀りが増えて、過去の整え方もぜんぶ読めます」は**在庫の案内**。道案内8本ができたら「**あなたの型の最初の3本から始められます**」を足せる。**8本が完成してから判断する**（先に足すと、行き先がない約束になる）。`CLAUDE.md` の恒久ルールなので、Cocoが「恒久ルールとして」と言うまで変えない。

### 【提案C】初月解約を、想定ではなく実測で持つ
`CLAUDE.md` は解約5%/月で「1,000人維持に月50人」と逆算しているが、**初月解約は一般に20〜40%**で、2か月目以降の解約率とは別物。**数字を推測で書き換えない**——`reference/weekly_metrics.json` の記入項目に「新規入会数」と並べて**「初月解約数」を1列足す**のが正しい対処。会員が出てから意味を持つので、**⑤会員10人を達成した月から記入を始める**。

- **順番は note.com ダッシュボードの週間ビュー降順。** 上位20本＝93ビュー／残り110本＝73ビューなので、読まれている順に効く
- **ボトルネックはCocoのnote.com手作業（130本ぶん）。** 途中で止まっても効果が最大になるよう、この順番を崩さない
- [ ] 第1バッチ（上位9本）
- [ ] 第2バッチ（上位10〜18本）
- [ ] 以降、型別に束ねて進める
- **完了の判定**：全130本で `note_check.py` 0件／全130本に型が割り当て済み／`note_com_edit_list.md` の手作業が反映済み


### 【完了】GitHubアカウントを Coco930118 に統合した（2026-08-30・9/1公開に間に合った）

**前提が判明**：`cocomethodology` は組織ではなく**2つ目の個人アカウント**で、「GitHub PagesとVercelを1アカウントで両立できない」という誤解から作られたもの。**実際は1アカウントで両立できる**（Pagesはリポジトリごと／Vercelもリポジトリごと。干渉しない）。今日の権限トラブルは全部これが原因。

**⚠️ 絶対にやらないこと：Vercelのプロジェクトを削除して作り直す。** 環境変数・KVの接続はプロジェクトに紐づいているので、**プロジェクトを保ったまま、Git連携先だけを差し替える**。作り直すと計測（KV）が飛ぶ。

- [x] **① バックアップ**：`cocomethodology/shindan` をzipでダウンロード（唯一の保険）
- [x] **② Vercelの現状を記録**（環境変数5つ＝REDIS_URL／KV_URL／KV_REST_API_URL／KV_REST_API_READ_ONLY_TOKEN／KV_REST_API_TOKEN。値は未表示のまま記録）：環境変数・ドメイン・ビルド設定・**KVの接続**をスクショ
- [x] **③ GitHub：Transfer ownership** `cocomethodology/shindan` → `Coco930118`（GitHubが自動でリダイレクトを張るので、Vercelの連携は多くの場合そのまま生きる）
- [x] **④ Vercelで連携先を確認**——**自動で `Coco930118/shindan` に追随した。手当てゼロ**：`Coco930118/shindan` になっていればOK。なっていなければ、**プロジェクトはそのままでGit連携だけ差し替える**（Vercel側で Coco930118 のGitHubを許可する必要があるかもしれない）
- [x] **⑤ 本番の状態を確認**——Status: Ready／Production Current／ドメイン `shindan-flax.vercel.app` 健在／プレビュー描画OK：小さな変更を1つpushして、デプロイが通るか確認
- [x] **⑥ KVが生きているか確認**——統計ページが週別集計を読めている＝接続OK：計測が動くか（`runbook_shindan.md` ③で読める）
- [x] **⑦ `cocomethodology/weekly`（フォーク）を削除**——削除前に検証：フォーク195ブランチ・タグ0で、**正典に無いものは0件**（唯一の差は自分の作業ブランチが1コミット古いだけで、正典の祖先であることを確認済み）
- [x] **⑧ Claude の GitHub 連携**——③と同時に解消。weekly と shindan の両方が見える → weekly と shindan の両方が見える状態にする
- [ ] **⑨ 水15:00の④-b ルーティンを登録**（Cocoの画面操作。`reference/runbook_shindan.md` の①週次反映を貼る）
  - **リポジトリは2つとも入れる：`Coco930118/shindan`（書く）＋ `Coco930118/weekly`（読む）。**weekly が入っていないと、そのセッションは診断の本文をどこからも取れず、必ず空振りする（2026-08-30 に試験実行して実際に起きた。「本文が含まれていなかったので何もデプロイしていない」で止まった＝**指示文としては正しい止まり方**だが、入力が届いていなかった）

**あわせて診断サイト側へ渡すこと**：診断の統計ページで、8/30時点の「今週」が **8/18–24** と表示され、**8/25–31 の週タブが出ない**。KVには w7（8/25〜）のデータがある。**週の区切りの計算がずれている疑い**——放置すると9/1公開後の数字を誤った週で読むことになる

**ロールバック**：④〜⑥でつまずいたら、**Transferは戻せる**（Coco930118 → cocomethodology へ再Transfer）。①のバックアップと合わせて二重の保険。**ここで詰むことはない。**

**9/1の公開は、⑥まで通ってから。** 統合と公開を同じ日にやらない。

### 【⑧で解消する】診断サイトのリポジトリが、Claudeから見えない（2026-08-30 検出）
ルーティンの「リポジトリを選択」で `shindan` が出てこない。**原因は権限**——Claudeが見えているのは `Coco930118` の6リポジトリだけで、`cocomethodology` の組織が入っていない（`list_repos` で確認済み）。

- [ ] **claude.ai の 設定 → コネクタ → GitHub を再接続し、`cocomethodology` を許可する**（All repositories か、`shindan` を明示的に選ぶ）
- **クラウド環境の名前を `cocomethodology` にしても、リポジトリの権限は付かない。** 環境名とGitHub組織は別物
- **それまでの回避策**：診断サイトの作業はルーティンにせず、Cocoが手でセッションを開いて `reference/runbook_shindan.md` から指示文を貼る

### note.comへのアップ
- [ ] **💼『温度の整え方 —— 組織と仕事』の常設案内 → 9/1**
- [ ] **💗『距離の整え方 —— 恋愛と関係』の常設案内 → 9/3**
  - どちらも書けているのに上がっていない状態が続いていた。メンバーシップを売る記事が有料だと読める人が会員しかいない、という理由で `public` 固定
  - 上げたらCocoから実際の公開日を受け取り、`date` / `publish_date` に入れて `published_confirmed: true` にする

### note.com側の貼り替え
- [ ] **`reference/note_fix_queue.json` の `open` 項目**（2026-08-27時点で33本）
  - サイトの「修正必須」フィルタから1本ずつコピーできる
  - **最優先＝8/26 subtract と 8/27 put-into-words**（死んだ導線・読者が探しても見つからない状態）。手順は `reference/note_com_edit_list.md` の末尾
  - 次に効く順：6/08 と 6/13（無料の入口）→ 6/26 → 12/03 Coco Methodology → 残りは順不同

### ルールの穴：**型診断（8問）の正典が weekly に無い**（2026-08-30 検出・Cocoの判断待ち）
2リポジトリ運用の設計は正しい——`Coco930118/weekly` が**言葉の正典**、`cocomethodology/shindan` が**サイトの実装**。診断サイト側のコミットも「**コンテンツのルールの正典をweeklyに一本化**（2026-08-25）」と言っている。

**ところが weekly 側に、型診断のルールが1行も無い。**
- `rules/shindan.md` にあるのは**入口の一行だけ**（`▼ 8問 / 約1分` の表記・接続の一行・橋渡しの一行）。X診断・Threads診断＝**日替わりの4択**のルールであって、8問の型診断のものではない
- `CLAUDE.md`「セッションの持ち場」の表にも**診断サイトの行が無い**（note／35投稿／診断の3つだけ）
- **つまり、4タイプの名前・軸の定義・結果画面の文言を決める場所が、いまどこにもない。** 型の名前が「HL／LH」のようなコードに寄っているとすれば、原因はここ（誰もルールを持っていない）

- [ ] **`rules/type.md` を新設するか**（型診断の正典：4タイプの名前／軸＝温度×距離の定義／結果画面(1)〜(4)の構成／型別の週次配信）
- [ ] **`CLAUDE.md` の「このファイルの読み方」と「セッションの持ち場」に、診断サイトの行を足すか**
- ⚠️ **恒久ルールの新設なので、Cocoが「恒久ルールとして」と言ったときだけ書く。** ここは提案のみ

### 【完了】診断サイトのアクセス数を取った（2026-08-30・結果は実質0）
KV接続済み・読み出し済み。**全期間の完了10件（恋愛9・組織1）、うち w7 の5件はCocoのテスト**（5件とも 08-31 入口・全部LL型）。**実データは事実上0。**
- 8/25週は**配信済み**。X診断のコメントは**140回表示**されているのに、`/soshiki` の開始は0
- **ただし「接続の一行が効かなかった」と結論しない。** 実読者はフォロワー35人規模で20〜40人。CTR2〜3%なら期待値は0.5〜1クリックで、**0は統計的に異常ではない**
- **入口（コメント→URL）を磨いても、母数30人では診断アクセスは月に数件が上限。** 次の一手は入口ではなく**出口＝結果のシェア導線**

### 型診断：総称は「間合い診断」で確定候補／実装と設計意図のズレ3件（2026-08-30）

**総称＝「間合い診断」**（正式名 `Coco Methodology 間合い診断`）。4型の中身を見て確信が強まった——**溶ける＝間合いが消える／秘める＝間合いが遠い／尽くす＝間合いは近いが自分がいない／守る＝間合いを固定している**。組織側も同じで、**全型が「間合い」1語で説明できる**。
- 説明の一行は「間合いの**調整**」ではなく「**あなたの間合いには、型がある**」。診断が映すのは調整法ではなく、**いまのクセ**

**実装されている型（サイトのデータより・確認済み）**
| 軸 | 恋愛 `/kankei` | 組織 `/soshiki` | 欠けているもの |
|---|---|---|---|
| 温度高×距離高 | **溶ける人** 30.7% | **背負う人** 27.4% | 自分の分が残らない |
| 温度高×距離低 | **秘める人** 15.9% | **こらえる人** 19.4% | 相手がいない（届いていない） |
| 温度低×距離高 | **尽くす人** 17.3% | **先に動く人** 26.0% | 自分がいない（渡すのが役割） |
| 温度低×距離低 | **守る人** 25.2% | **降りる人** 23.8% | 閉じたまま動かしていない |
| 中央 | **保つ人** 10.9% | **保つ人** 3.4% | ※CTAを出さない |

**ズレ①：型の名前**。Cocoの認識は「しずく型／しらたま型／ひより型／ブレンド型」だが、**実装は動詞人称（溶ける人・守る人…）**。
- **動詞人称を型の名前として採るのが正しい。** 理由3つ：①動詞人称は**痛みを名指ししている**ので「それ私だ」が立つ ②キャラ名は可愛いが自己認識が立たない ③**原理06（自責を特性に反転）が効くのは動詞のほう**——「わたしはダメ」→「わたしは溶ける人」は反転になるが、「わたしはしずく型」は反転にならない
- 血液型のABを真似た「ブレンド型」の心配は、**実装ではすでに「保つ人」になっていて解決済み**。「ブレンド」は"どっちつかず"と読まれるが、「保つ人」は名乗れて否定的でない。**AB型が成立するのはA/B/Oが先に全員に浸透しているからで、新規の診断では中間型は最初から弱い**

**ズレ②：5型に3匹は当たらない**。4型＋中央＝5、ドメイン2つで計10。キャラは3匹。**1:1に当てるのは構造的に無理。**
- **提案：キャラは「型」ではなく「声」に当てる2層にする。** 元のキャラ設計（担当ステージ）がそのまま使える——しずく＝内省・整える／しらたま＝自己受容・感情ケア／ひより＝選択・飛び立つ
- **型＝いまの立ち位置（変わらない・名乗れる）／声＝いま要る関わり方（変わる・毎週更新できる）**
- これは `CLAUDE.md`「**毎週増えるのは場面、変わらないのは原理**」と同じ構造。そして**条件5（型が毎週の配信の宛先になる）が自然に実装できる**：「今週の、溶ける人へ」＋そのときの声

**ズレ③：「保つ人」にCTAを出さない**＝恋愛の10.9%が行き止まり。CTAを出さない判断自体は誠実で正しい。
- **提案：CTAの代わりに、もう片方のドメインへ送る。** 恋愛で保つ人だった人は組織で受ける（逆も同じ）。売り込みにならず、**1人が2つの型を持つ設計そのものを活かせる**

**1人が2つの型を持つ構造は、MBTIにない強み**。`coco_methodology.json` の読者定義「一件片づいても終わらない。**職場と家で同時に走る**」がその根拠。名乗り文が「恋愛は溶ける人／仕事は降りる人」になり、**MBTIより情報量が多く、話が広がる**＝シェア文脈で強い。

- [ ] 総称「間合い診断」の可否（Coco）
- [ ] 型の名前を動詞人称に一本化してよいか（Coco）
- [ ] キャラを「声」に移す2層設計の可否（Coco）

### ルーティン化：診断サイト側の指示文をつくる（2026-08-30 Coco要望）
`Coco930118/shindan`（当時 `cocomethodology/shindan`）はいま単発セッション。ルーティンにするには指示文が要る（weekly の `reference/runbook_weekly.md` にあたるもの）。

**分業の形（Cocoの流れ「weeklyで作って、cocomethodologyに伝えてサイト仕様で上げる」をそのまま手順にする）**
- **weekly側の指示文**＝**言葉を決める**。型の名前・結果画面の文言・接続の一行。正典は `rules/`
- **shindan側の指示文**＝**渡された言葉をサイト仕様に落として上げる＋KVの計測を読む**。**ルール本体を持たない**（`CLAUDE.md` の「指示文はルール本体を持たない」と同じ原則）
- **なぜ分けるか**：shindan のセッションは weekly を読めない（別オーナー）。**shindan側の指示文にルールを書くと、weeklyと二重持ちになって必ず片方が古くなる**——今日の点検で出た高10件の半分がこの形だった
- [ ] weekly側：`reference/runbook_weekly.md` に診断サイトの回を足すか、`reference/runbook_shindan.md` を新設するか
- [ ] shindan側：受け取り手順・デプロイ手順・計測の読み方だけを書いた指示文
- ⚠️ どちらも恒久ルールに触るので、Cocoが「恒久ルールとして」と言ってから書く

### 検査の穴（Cocoの判断待ち）
- [ ] **funnel返信2の約束語と、対応noteの本文を突き合わせる検査がどこにも無い**
  - 2026-08-27に死んだ導線が5本まとめて出た原因。`self_replies[1]` の「AとBをnoteに書きました」の両側を取り出して本文を検索するだけで検出できる
  - **恒久ルールにするならCocoの「恒久ルールとして」の一言が要る。** Wチェックが「ツールの変更はWの権限」としているので、W側でやるならnoteセッションは何もしない

---

## 診断セッションへ渡すもの（持ち場外・2026-08-30）

### 【急ぎ・9/1公開に要る】9/1週の診断14本に `axis_map` を足す

診断サイトが 9/1週の結果ページを組めずに止まっている。**足りないのは配点だけ**（DAY_ANSWER と TYPES の原文は既に weekly にあることを確認済み——`comment` / `reply_1` / `rules/type.md`）。

- [ ] `posts/shindan_x_2026-09-01.json` の7本に `axis_map` を足す
- [ ] `posts/shindan_th_2026-09-01.json` の7本に `axis_map` を足す
- 形は `rules/shindan.md`「`axis_map`」。**4つの選択肢が4象限に1つずつ**入るのが基本。同じ象限に2つ入る回は選択肢のほうを直すか、報告する
- **8問診断の1問目と同じ物差しで付ける**（日替わり4択はQ1なので）
- 8/18週・8/25週は既にサイトに反映済みなので、遡って足さなくてよい
- **4隅に1つずつ割り当てる。中央（保つ人）は使わない**（`rules/shindan.md`。旧実装は3隅＋中央で、1択が結果を持てなかった）
- **`axis_map` は配点だけでなく「記号→型」の橋渡しそのもの。**サイトの `DAY_ANSWER` は型キー（HH/HL/LH/LL）で引かれ、weekly は選択肢キー（A〜D）で持つ。これが無いと結果画面は組み上がらない

### 【急ぎ】TYPESの本文が正典に無い（`rules/type.md` は型名と「欠けているもの」まで）

**2026-08-30 訂正（2回目）**：「TYPES の正典は `rules/type.md`」と書いたが、そこにあるのは**型の名前と「欠けているもの」だけ**。一行タグの列すら無かった。**`tag` / `punch` / `ans` / `cta` / `bridge` の原文は、いずれも正典に無い**——サイトが持っている（w7から持ち越し中）。

- [ ] **10型ぶんの TYPES 全フィールドを `rules/type.md` に正典化する**（診断セッションの持ち場）。サイトの `data-wN.js` から書き起こして、weekly を正にする
- [ ] **組織側の「欠けているもの」3つを実装から書き起こす。**実装との照合は恋愛4/4合致・**組織は1/4**（合っているのは こらえる人 だけ）。背負う人・先に動く人・降りる人の3つは、実装の文言（「渡せたはずの一件」「判断のほうを渡していない」「一度も情報を戻していない」）のほうが具体で、**組織の声（冷徹な軍師）に合っている**。実装を正としてよい
- [ ] そのとき「あなた」を落とす。**現在サイトに kankei 7箇所・soshiki 7箇所残っている**（`rules/shindan.md` の主語なしルールは9/1週から適用で、射程に結果画面のコピーが入る）
- [ ] **中央（保つ人）の「もう片方のドメインへ送る」文言を作る。**サイト側はCTAを消して `crossover: { body, cta }` の枠だけ用意済み（2026-08-30 完了）。**その枠に入れる2文を作る。**中央は「まだ型が定まっていない人」なので、売らずに逆側のドメインへ送るだけ
- [ ] **中央の共有文の「配合で生きてきた人」を見直す。**「ブレンド」の語は消えたが、**その日本語訳が残っている**。`rules/type.md` が「ブレンド型」を退けた理由（中間型は「どっちつかず」と読まれる）は、この語にもそのまま当たる。**語を置換しても概念は消えない**という例（2026-08-30 発見）
  - **9/1週は残す。**言葉を作る作業を公開日に挟まない。TYPES正典化と同時に直す
- **9/1週は持ち越しのまま出してよい**（14箇所の「あなた」を残したまま公開する。**言葉を作る作業を公開日に挟まない**）

### 【中】Q8（サイト限定の8問目）に出典が無い

8問診断は「その週の日替わり4択7問＋サイト固有のQ8」で組まれている。**Q8だけ毎週書き直されていて、weekly に原文が無い**（w6→w7でフック文が変わっている）。

- [ ] **Q8を固定して `rules/type.md` に正典化する。**8問診断は常設なので、毎週変える理由がない
- 9/1週は w7 の逐語コピーのままでよい

### 【中】診断サイト側の `CLAUDE.md` がルールを持っている

サイト側の `CLAUDE.md` に §③「③で処方を渡さない（処方は④⑤＝有料の領域）」がある。**これは旧版で、weekly の「拡散帯は結論まで無料で渡す」と正面から衝突する**（「説明は無料、処方は有料」は 2026-08-30 に `rules/source.md` から削除済み）。

- [ ] **サイト側の `CLAUDE.md` から言葉のルールを全部落とす。**サイト側が持ってよいのは実装だけ（データ形式・ページ・計測・デプロイ）
- [ ] 落とす前に、**サイト側 `CLAUDE.md` の全文を weekly に読ませて、正典と食い違う条項を洗い出す**（今回見つかったのは §③だが、他にもある可能性が高い）
- **これが二重持ちの典型。**指示文からルールを外しても、リポジトリ側の `CLAUDE.md` に残っていたら同じこと

### 【中】過去の `data-wN.js` の TYPES が週ごとに割れている疑い

診断サイト側の報告：w6 と w7 で型別説明文が違う。**型の説明は `rules/type.md` が正典で、週で変わるものではない。**サイト側が毎週書き直していたなら正典違反。

- [ ] 診断サイト側の④全週監査で**差分を出させる**（直させない・報告だけ）
- [ ] 出てきた差分を見て、`rules/type.md` に揃えるかCocoが決める

---

## 試験中（実測が出たら判定する）

### 【レバーA】返信と引用（2026-09-01〜・X＝引用／Threads＝返信）

**2026-08-30 訂正**：当初「拡散帯17本のうち5本を返信に振り替える」と設計したが、**前提が誤り**だった。予約投稿と返信・引用は**別の層**で、片方をもう片方に置き換える理由がない。**週49本は減らさない。**書き方の正典は `rules/posts.md`「返信と引用の書き方」。

**実施状況**：X＝引用を開始済み／Threads＝返信を随時開始予定（Coco）。

**測ること（9/29に見る）**
- **主指標＝X・Threadsそれぞれのフォロワー週次純増。**返信・引用そのものの表示数は取れないので、これで測る。ベースラインは8月の実績（X 35／Threads 40）
- **副指標＝予約投稿の表示が落ちていないか**（拡散帯の中央値）。返信・引用は上乗せなので理屈上は落ちないが、確認する
- **X と Threads を分けて見る。**Xは引用（自分のフィードにも出る）、Threadsは返信（出ない）で、届き方がそもそも違う。片方の結果をもう片方に適用しない
- `reference/weekly_metrics.json` に、その週に何本つけたかを Coco が一言で記録する（正確な数でなくてよい。「今週は引用5・返信3」程度）

**判定の分岐**
- 純増が上がった → 頻度を上げる。`rules/posts.md` の下限「1日1本」を引き上げる
- 変わらない → **やめる判断はしない**（露出面を失わない施策で、コストはCocoの時間だけ）。相手の選び方を変えて4週延長する
- 予約投稿の表示が落ちた → 上乗せではなく食い合っている可能性。そのとき初めて本数を考える

**Cocoが恒久ルールにすると言えば、`rules/posts.md` の節はそのまま恒久ルールになる**（いまは「2026-09-01〜」の日付つきで置いてある）。

---



判定に使うのは `reference/weekly_metrics.json` の実測だけ。**中止と拡大はCocoの判断。** 判定した週は、結果と用量の変更を作業報告に書く。

### note無料枠を「組織側から3週連続」で置く（2026-08-30 Coco承認・期限つき試験）
**根拠（2週連続の実測）**：noteで読まれているのはほぼ組織。上位20本の内訳は 8/24〜8/30週が 💼組織16本=72ビュー／💗恋愛1本=8ビュー（テーマで数えると 85/93＝**91%が組織**）、前週も 組織79／恋愛24（77%）。在庫は恋愛66本・組織40本で**恋愛のほうが1.65倍多いのにこの差**。恋愛の読者は「検索しない人」（`CLAUDE.md` 読者の状態）なので、note内検索・おすすめから恋愛noteに来る経路が構造的に細い。

- [ ] **9/1週・9/8週・9/15週の無料枠（型A・まとめ記事）を、3週とも💼組織で作る**
- **この試験中だけ、`rules/note.md`「プランを毎週交互にする」を停止する。** 恒久ルールは変えていない——試験が終わったら交互に戻すか、Cocoが「恒久ルールとして」と言ったときだけ書き換える
- **判定（9/22時点）**：note週間ビューと、上位20本の組織／恋愛の内訳。伸びなければ交互に戻す。伸びたらCocoに恒久化を提案する
- 型A（既存の有料記事8〜10本を一つのテーマで並べる）・`visibility: public` ／ `price: "0"` ／ `free_ratio: "1.0"` ／ `tier: "free_entry"` は既存ルールのまま
- **深さの上限＝X・Threadsに載せる深さ（2026-08-30 Coco決定）。** 無料まとめは**深堀りではなく入口**で、興味を持ってもらうところまで。深堀りは全部有料側に置く。これは既存の型Aルール「中身は要約まで、処方は各記事へ送る」と同じ線を、**深さの基準で言い直したもの**

### Threads診断14:00の「中身」を変える（2026-08-30 Coco決定／停止案は不採用）
**まず撤回**：「7本を止める」提案は不採用。理由は2つとも妥当——①**拡散していない段階で露出面を削る手はない** ②「低表示の投稿がアカウント全体の配信を食う」仮説の根拠は**1週間の相関だけ**で、因果は示せていない。削って上がるのは仮説が当たっていた場合だけで、削って確実に失うもの（週7本の露出面・シリーズの連続性）のほうが大きい。**本数は動かさない。**

**疑う先を、本数から中身に移す。**
- 実測：コメント到達率 18%（8/18週）→ **11.4%**（8/25週・新形式）。7日中4日はコメント0。同じ週に**X診断は 103% → 177% へ改善**している
- **仮説：Threads診断には「持ち帰れるラベル」がない。** いまの形は「その場面でどうする？」の4択で、答えはコメント側にある＝**開かないと終わらない構造**。組織の読者は正解を求めるのでコメントを開く（X診断177%）が、恋愛の読者が求めているのは正解ではなく**自分の名前**
- **打ち手は「型診断」への作り替え**（下の項目）。診断サイトの `/type`（3匹の型診断・9/1公開予定）と統合できる可能性がある
- 判定：作り替えた週のコメント到達率と、保存・引用の増減

### オリジナルの型診断＝**もう作ってある**（2026-08-30 現物を確認・出すのが先）
**Cocoの発案**「MBTIとか個性学みたいに、枠に入れて安心することで、自分ではなく特性ととらえて前へ進める糧になるオリジナルなもの」——**これは診断サイト（`shindan-flax.vercel.app`）に既に実装済み**。リポジトリは `cocomethodology/shindan`（private・118コミット・115デプロイ）。

**確認できた設計（サイトのトップとリポジトリのコミット履歴より）**
- 軸＝**温度 × 距離**。サイトの導入文が「感情の温度と、相手との距離。**その2つの立ち位置**にある。温度も距離も、**職場にも恋愛にも、両方ある**」
- リポジトリ説明＝「関係の温度と距離を整える技術 —— **タイプ診断**」
- コミット「**型とキャラの対応を修正（HL↔LH：秘める/こらえる→しず…）**」＝2軸2値の4タイプ＋3匹のキャラ対応
- コミット「結果画面に(4)「**ここまでで、下ろせるもの**」を追加」
- コミット「1問目に「**投稿で見た、あの問い。**」の認識の一行を追加」＝日替わり4択との接続済み
- コミット「**OG画像を全98枚キャラ主役に統一**」＝シェア用の画像が98枚ある
- コミット「計測バグ修正：第5週・第6週が**KV**に記録されていなかった」＝計測機能あり

**5条件での評価**
| # | 条件 | 判定 |
|---|---|---|
| 2 | 軸がまだ名前のついていない領域か | **◎ 温度×距離。MBTI（性格）・ストレングス（才能）・動物占い（生年月日）のどれとも被らず、しかも商品名そのもの** |
| 4 | 型が決まった瞬間に今日やめられることが1つ渡るか | **◎「ここまでで、下ろせるもの」がそれ** |
| 3 | どの型も負けていないか | 未確認（診断共通ルール2があるので満たしている見込み） |
| 1 | **名乗れる名前があるか** | **未確認・最重要。** 結果が「HL型」なら名乗れずシェアされない。「しずく型」なら名乗れる |
| 5 | 型が毎週の配信の宛先になるか | **未実装と思われる。いちばんの伸びしろ** |

**設計は正しい。問題は流通。** 診断サイトは**6週連続アクセス0**で、noindex＋TOP導線なしで9/1公開待機中。OG画像を98枚用意していても、シェアされる以前に誰も辿り着いていない。

- [ ] **9/1に noindex を外してTOP導線を開く**（予定どおり）
- [ ] **結果画面の「型の名前」が、口に出して名乗れる形になっているか確認する**（条件1・最重要）
- [ ] **8問への誘導の一行を、コスト表示から報酬表示に変える。** いまの「▼ 8問 / 約1分」は「あと7問やらされる」と読める。X診断のコメントは到達率177%で**読まれているのに踏まれていない**＝踏む理由が足りていない。**何が手に入るかを名詞で見せる**（例：「あと7問で、職場でも恋愛でも繰り返している自分の型に、名前がつく」）
- **この型診断は、2つの柱を1本で串刺しにできる唯一の資産。** 1回診断して1つの型を持てば、組織の悩みにも恋愛の悩みにも効く＝**両プランが同時に自分ごとになる**。並行運用のコスト（変数2倍・ルールの4象限）を、強みに変える構造
- ⚠️ **作業は診断サイト側の持ち場。** `weekly` のセッションからは読めない（別オーナーのため接続不可・サイトもネットワーク遮断）。**`cocomethodology/shindan` を起点に別セッションを立てて進める**

### 旧noteの再編集を続ける＋どれを編集したか記録する（2026-08-30 Coco報告・いちばん確実なROI）
**Cocoの報告**：「昔の記事を今のルールで再編集しようといじったらアクセスが上がった。**まだ編集が追いついていない**」

- **実測が裏づけている**：note週間ビュー 29（8/18週）→ **170**（8/25週・8/24に再編集）→ 166（横ばい）。**跳ねたのは再編集の直後、止まったのは編集が止まってから**
- note.com は更新した記事を再評価する（検索インデックスの再取得・おすすめの再判定）。上位20本の1位が8か月前の記事（2025-12-15）なのも、新規性ではなく**更新が拾われている**ことの傍証
- 在庫130本に対して編集済みはごく一部。`reference/note_fix_queue.json` は69件を抱えている
- [ ] **その週に再編集した記事のタイトル一覧を、実測ログに残す。** いま記録がないため、**5.9倍の再現条件が分からない**。「編集した本数」と「翌週のビュー」が並べば、1本あたりの効き目が出る
- **新規9本の一部を再編集に振り替えるかは、この記録が2週たまってからCocoに提案する**（配分の変更なので実測が要る）

### 【完了】計測の定義を改定した（2026-08-30 Coco決定・Wチェックが反映）
`weekly_metrics.json` の `metrics_definition` は「本文表示は配信からの経過日数にほぼ比例する」としているが、**Threadsでは成立していない**。8/25の3本は配信1日後（26／64／42）から5日後（27／65／43）まで **+1ずつ**しか増えていない＝**表示はほぼ24時間で確定する**。
- [x] **①「経過日数に比例」をThreadsについて撤回**／**②主指標を平均から中央値へ**。`weekly_metrics.json`（how_to・metrics_definition・8/25週の change_log）と `rules/shindan.md` に反映済み
- **射程はThreadsだけ。X・診断の本文表示は未検証**——同じ形（day1とday5）で一度測るまでは経過日数を添え続ける
- **申し送りとのズレ**：2026-08-30の申し送りは「計測ドクトリンが2つ変わった」と書いていたが、リポジトリ側はこの節が「Cocoの判断待ち」のままで、旧ドクトリンが4箇所に残っていた（weekly_metrics 3箇所＋shindan.md 1箇所）。**申し送りが決定済みと書き、実体が未反映**という形だったので、Wチェックで揃えた

### X診断のウィット一滴（週2〜3本）
- **適用開始は 9/1週（#087・#089・#091）。8/25週は適用されていない**（2026-08-29 Wチェックで実態を確認・Coco承認）
  - 8/25週の #078・#083・#084 は、スロット（結びの後・接続の一行の前）は埋まっているが、中身は**Cocoの実体験の一行**で、ウィット一滴ではない（例：#084「こっちだけ心臓が痛い日が、何度もある」）。ルールの「主語なしの軽い一行・整った側の風景」に当たらない
  - 9/1週の3本は素材（E373）由来で条件を満たす（例：#089「週末は温泉。浸かっている間は、何も考えていない」）
  - **X診断は内容を変更しないため、8/25週は直さない。記録のほうを実態に合わせた**
  - 2026-08-25 のWチェックが「実態に合わせて訂正」として 8/25週開始に書き換えていたが、これが誤り。当初の「9/1週開始」が正しかった
- **9/1週・9/8週の2週を回したら**、ウィット入りの回とそれ以外を比べて、用量の増減・継続・中止をCocoに提案する（8/25週は適用週でないので比較に使わない）
- **比較は「非適用回との比較・8割」に一本化**（2026-08-29 Coco決定）。基準の本体は `rules/shindan.md` ⑤の試験の判定基準。旧「7本の中央値」は廃止——閾値のある基準と無い基準が並立していた

### Threads診断のウィット一滴（週2〜3本）
- **数えてよいのは、生活の具体が実際に描かれている一行だけ**（2026-08-29 Coco決定。条件は `rules/posts.md` ルール4①）。ツールが出すのは「候補」で実数ではない
  - **8/30「決めてしまえば、あとは普通の日を過ごすだけでいい。」は数えない**（生活の描写がなく、まとめの一行と区別がつかない・2026-08-29 Coco決定）
- 8/25週は**8/27の1本のみ成立**（「だから今夜は、家族の気持ちを確かめなくていい。いつも通りの席で、ごはんを食べればいい。」）。9/1週は9/2の1本
  - **8/27の一行は `rules/shindan.md` の例文と完全に同じ。** 次からは中身を変える（既視感ゼロチェック③・「中身は毎回変える」に抵触）
- 用量が週1本で推移しているため、判定に必要な本数（週2〜3本）に届いていない
- 判定は**コメント表示**で見る（ウィット行がコメント側にあるため）
- 継続＝ウィット適用回のコメント表示の平均が非適用回の8割以上／8割未満＝翌週1本に減らして再試験／**2週連続で8割未満なら中止をCocoに提案**
- コメント到達率が2週連続で3割未満なら、本文への移動をCocoに提案（最後の手段）

### 逆説の言い切り型（拡散帯・週1〜2本）
- 初回＝2026-08-25週 th_21「大切にされたい／大切にしたい」
- **2週続けて拡散帯の中央値を下回ったら中止。上位に入ったら配分に正式昇格**し、型配分の目安（実験10・場面4・問い3）を実測で組み替える

### 未完成の問い（Threads週2本・9/8週から・2026-08-27 Coco承認）
背景：外部レビューの提案。「完成度が高いぶん引用しづらい。読者は『わかる』で終わる。返信が付く投稿がインプレの母艦になる」。**ただしこの口座の実測（3,299・1,211・969）はいずれも完成稿**なので、**試験以上にしない。勝たなければ廃止**。

- [ ] **Threadsのみ・週2本。** Xは末尾を明示質問にしない恒久ルールがあるため対象外（`full_check.py` が検出）
- **置き場所は「読者に戻す一言」のスロット**（終盤の並びの5番）。締め（名前をつける一行）は動かさない。**締めを問いにしない**
- **形**：処方を渡したあとに、答えを閉じない問いを1つ置く（例：「決めているのが向こうだと気づいたあと、先に何をやめた？」）
- **判定**：9/8週・9/15週の2週で、問いを置いた回と置かなかった回の**拡散帯の中央値**を比べる。**返信数も見る**（インプレの母艦になるという仮説なので、返信が増えなければ仮説が外れている）
- **勝てば週2〜3本で継続。負ければ廃止**（配分に昇格させない）

### Threads拡散帯の「前へ進む力」3点（9/8週の作成時に試験投入・2026-08-26 Coco承認）
背景：8/25週のThreadsが「後ろ向きのつぶやき」に見えた（Coco指摘）。実データで数えたところ、ルール違反はゼロで**用量の偏り**だった——「昔」開き5本（うち2本が19:00枠の外）、ぼかし締め9本（43%）、伝聞三連鎖（8/25〜26のth_01・02・04）。一方、**初速4本の実測では「んだよね」締めが1位・2位**で、語尾は原因ではなかった。残った仮説が下記①。

- [ ] **① 冒頭2行に、体が動く絵を1つ入れる**（最有力）
  実測の根拠：th_02（64）＝立つ・行く・飲んで戻るの3動作／th_04（57）＝夕飯を先に決めた／th_01（26）＝「聞くのをやめた」＝行動の不在。伝聞・実験構造・語尾まで同型で、差は冒頭だけだった
- [ ] **② 伝聞マーカーを散らす**（「〜人がいる」「〜って」「〜みたい」）。1本2回まで・**同日および3本連続で並べない**。
  理由：語り手ルール（現在進行の恋愛の試行錯誤はわたし目線で書かない）を守ると実験が全部相談者に振られ、「わたしはこうしてる」の現在形が実験を一度も担わない週になる。ルールは崩さず、配置で散らす
- [ ] **③ 「昔は／昔のわたし」開きは19:00「昔と今」枠のみ**（8/25週は07:00・10:00に2本漏れた）
- **判定**：8/25週（ぼかし・伝聞多め）と9/1週（言い切り12本・「昔」開き2本）が自然なA/Bになっている。**両週の実測が揃った時点で拡散帯の中央値を比較**し、①②③の本採用・用量をCocoに提案する。8/25週の中身は途中で直さない（比較が壊れるため）
- **やらないと決めたこと**：拡散帯の命令形解禁／CTAでの「変われる」約束／権威の前出し。力強さは**わたしの決断の描写**だけで出す（読者への号令は入れない）

---

### ウィット一滴の目視実数（2026-08-29 Coco決定を反映）

機械の「候補」と目視の実数がずれる。**目安の判定は目視の実数で行う。**

- **9/1週 35投稿＝目視4本**（x_08 ビール／x_09 昼寝／th_04 アイス／th_13 おいしいもの）。目安5〜6に対し**2本足りない**
  - `x_11`「昼は散歩にでも出て、守るのは寝る前の一分だけ」は**数えない**（処方の一行に溶けている）
  - `th_14`「やっているのはそれだけ。」は**数えない**（語が当たっただけで生活の描写がゼロ）
- [ ] **9/1週は未配信なので、ウィット一滴を2本足せる。** 35投稿セッションに渡す
  （E373の未使用要素は `python3 tools/episode_check.py --week 2026-09-01` の末尾に出る）

### 9/1週の残り：④は直す／⑤は9/8週に持ち越す（2026-08-29 Wチェック決定）

グローアップからの申し送り。**1本ずつ直せるものだけ9/1週で直し、配分の偏りは次の生成に持ち越す。**

- [ ] **④ 1本ずつ直せる5件 → 35投稿セッションへ**（指示文を出した）
  1. th_10（9/4）th_01 と骨格が重複。完全一致3箇所＋reply2 がほぼ同文
  2. th_19（9/7）E233 に無い「改札／二十分／一度も時計を見なかった」。E233 は在庫中もっとも厳しい verbatim_rule
  3. th_16（9/6）「十分／三分」＝素材に無い経過数字（E328 は思考の型で数字を持たない）
  4. x_13（9/7）「月曜は少しだけ早く着くことにしてる」が在庫に無い（reference/*.json 全文検索でヒット0）
  5. th_07（9/3・funnel）funnel回に伝聞。9/1週の funnel 8本でここだけ

- [x] **⑤ 配分の偏り4点は 9/1週で直さない。9/8週の生成に効かせる**
  - X判定先出し「AではなくB」が9本（前週4本から倍増）／処方クラスタ「書き出して絞る」X4本／
    原理06への偏り（X7本・2週連続で増加）／週またぎの処方クラスタ2週連続3本
  - **直さない理由は3つ**：
    ① **A/B試験が壊れる。** 9/1週は「前へ進む力」3点の片翼（上記の判定を参照）。
       大きく書き直すと 8/25週との比較ができなくなり、①②③の本採用を判定できない
    ② **規模。** ⑤を全部直すとX14本の大半を書き直すことになる。
       2026-08-29 の実績では、ルールの二重持ちを直す作業だけで新しい矛盾が3件出ている。
       大規模な書き直しは、それ自体が新しい違反を作る
    ③ **配分は次の生成で作り込むほうが速くて確実。** 1本ずつの違反ではなく用量の問題なので、
       出来上がったものを削るより、作るときに散らすほうが自然な文になる
  - [ ] **9/8週の生成時に効かせる**：X判定先出しの構文を週3本以内／
    「書き出して絞る」クラスタを週3本以内（todo.md 既出）／原理06をXで4本以下・原理03を増やす／
    週またぎで同じ処方の動詞に寄せない

## 週次ファイルの削除（保持期間30日）

「その週の最終日＋30日」を過ぎたら、`posts/index.json` から外してファイルを消す。診断ファイルも同じ週のぶんをセットで。noteも同じ。

- [ ] 8/18〜8/24週 → **9/23以降に削除可**
- [ ] 8/25〜8/31週 → **9/30以降に削除可**

常設案内note（`source_week: standing_guide`）と過去分（`week_group: 過去分`）、`notes/drafts/` は対象外。

---

## 在庫がなくて作れないテーマ

素材が渡されるまで、投稿・note・診断を作らない（`reference/episode_usage_log.json` の `no_stock`）。

- 円満退職の手順
- がっつかずに距離を縮める塩梅
- 憎まれない別れ話

---

## 次の週の作成時に効かせるもの

- [ ] **9/1週作成時：「一行書く／数える」処方クラスタを週3本以内に**
  （8/25週は x_03・x_09・x_12・th_15・th_19 の5本に分散した実績あり。
  語の検索では検出できないため、処方の言い換え重複チェックで目視する）

---

## 他セッションに渡したもの（noteセッションの持ち場外）

（いまは無し。佇まい枠の素材違反2本は 2026-08-29 に完了を確認して削除——8/25 th_14 は「昭和ドラマ」、9/1 th_15 は「ヨガ」（花はX診断9/3と週内で重なったため 2026-08-29 に再差し替え）、9/1 th_21 は「ベランダ」1要素に。いずれも `episode_id` が E373。あわせてCocoから**ベランダ・昼寝**をE373に追加受領したので、この2要素は今後そのまま使える）

---

## 順番待ち

- [ ] 7月以降の note に有料ラインの行を入れる（Cocoが手で設定済みのため優先度は低い）
