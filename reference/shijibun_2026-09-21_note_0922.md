# 指示文｜9/22週のnote —— 新規生成はしない。funnel返信2に当て直す（noteセッション）

> **一度きりの指示文。済んだら消す。**
> **生成の手順の正本は `reference/runbook_weekly.md` の ⑦。** この指示文にルールを書きません。**本文を足す段になったら ⑦ をそのまま回してください。**

## まず結論：9本はもうあります

**⑦ は 2026-09-20 に走っています**（`5371bdc`「9/22週のnote9本を生成した」）。flagship 8本＋無料まとめ1本で、`notes/index.json` と `reference/note_archive.json` にも登録済みです（一覧は `notes/index.json` を見てください。**ここに写しません**）。

**新しく9本作る作業ではありません。** 開いているのは下の1点だけです。

## いま開いていること（実測 2026-09-21）

`python3 tools/note_check.py --week 2026-09-22_2026-09-28` → **要修正11件。11件すべてが、投稿の「約束」とnoteの `promise_map` のズレです。**

原因は一つ。**2026-09-21 の Final Editor が、funnel 8本の返信2を8本とも書き換えました**（note生成時点の `5371bdc` と比較）。**noteは書き換え前の返信2に対して書かれています。**

| 出ていること | 件数 | 持ち場 |
|---|---|---|
| **`promise_map` が投稿の約束と違う**（x_02・x_03・th_05・th_11） | 8件 | **noteセッション＝この指示文** |
| **funnel返信2から約束を取り出せない**（x_05・x_08・th_03） | 3件 | **35投稿セッション。** 返信2の1行目が中身の名指しではなくなっています（順の正典は `rules/posts.md` ルール3④） |

**この3件は直せません。報告に「持ち場外」と書いて残してください。**

## ⛔ 走れる条件

**返信2が確定してから当て直します。**

- 上の3本（x_05・x_08・th_03）を35投稿セッションが直すと、**約束の文がもう一度変わります。** 先に当てると二度手間です
- **9/22週の投稿は、いま要修正51件**（`python3 tools/full_check.py posts/week_2026_09_22_2026_09_28.json`）。**うち8件は「funnel返信2に定番プロミスなし」で、funnel 8本すべてに出ています**
- **funnel 8本の返信2に要修正が0件になってから**始めてください。0件でなければ、当てずに「返信2がまだ動く」と報告してください

## 対象（4本）

| note | 回収する投稿 |
|---|---|
| `notes/note_2026-09-22_fact-not-person.json` | x_02 |
| `notes/note_2026-09-23_no-answer-given.json` | x_03 |
| `notes/note_2026-09-23_waiting-to-absorbed.json` | th_05 |
| `notes/note_2026-09-25_no-permission-needed.json` | th_11 |

## 手順（1本ずつ）

1. **投稿の返信2を読んで、約束をそのまま写す。** 割り方は `tools/note_check.py` の `promise_parts` が持っています（`と、` が単位の区切り）
2. **その約束を、noteの本文がいま回収しているか確かめる。** 回収している見出しを `promise_map` に書く
3. ⚠️ **回収していなければ、`promise_map` を埋めないでください。** 本文を足すか、投稿の約束を戻すかは**Cocoの判断に返す**（`CLAUDE.md`「修正が必要なところは、提案だけ」）——**空手形を機械で通す形にしない**（`note_check.py` の `funnel_check` に 2026-08-31 の事故の記録があります）
4. **本文を足すことになったら、そこから先は ⑦ のとおり**（在り方署名・6項目・タイトル・画像プロンプト・md/html同期）
5. `python3 tools/note_check.py --week 2026-09-22_2026-09-28` を回す。**このセッションで消せるのは8件まで。** 残る3件は35投稿セッションの持ち場なので、**件数と理由を書いて出す**

## 触らないもの

- **`posts/week_*.json`**（返信2も含めて35投稿セッションの持ち場）
- 9/15週以前のnote・診断・35投稿
- `rules/` と `CLAUDE.md`
- **既存note全書き直しのプロジェクト**（⑦「この枠と同時に走らせない」）

## 出力

- **走る前の `note_check` の件数と、走ったあとの件数を両方書く**
- 4本ぶん「note名 → 投稿の約束（写し） → 回収した見出し → 回収できていれば `promise_map` に入れた／できていなければCocoに返す」
- **持ち場外の3件を、理由つきで並べる**
- 本文を足した場合は、足した箇所と素材ID（`episode_id`）

## 申し送り

⚠️ **⑦ が走ったあとに Final Editor が返信2を動かすと、noteの約束は必ず置き去りになります。** 同じ形が、同じ週のX短文でも起きています（`reference/shijibun_2026-09-20_xshort_0922.md`）。**2件目なので `reference/todo.md` に注意として置きました**（`CLAUDE.md`「ルールは v1.0 で凍結。足す前に再発を数える」の2回目）。**恒久ルールにするかどうかは、3回目が出てからCocoが決めます。**
