# 申し送り（2026-09-02）

**寿命つきの文書。** 担当セッションが消化したら、その項目を削除する。**ここにルールを書かない**——ルールの正典は `CLAUDE.md` と `rules/` だけ。ここにあるのは「何が起きているか・どこを直すか・誰が」だけ。

---

## ① Wチェック／指示文を書く側へ｜指示文が、正典に無い枠組みで条件を出している

**2026-09-02 の35投稿セッションで、同じことが3回起きた。**

指示文に次の語が出てくるが、**`CLAUDE.md` にも `rules/` にも `reference/` にも、全ブランチにも存在しない**（`grep` で確認済み）。

| 指示文の語 | 正典での検索結果 |
|---|---|
| **「7段のD」「Dをどこに置くか」**（`rules/posts.md` が正典と明記されていた） | 該当節なし |
| **「7段のG」** | 「7段」という語がゼロ |
| **「Cの適用」「相手のねじれ」** | どちらもゼロ |
| **「5段そろわなくていい回」** | 「5段」という語がゼロ |

※ A〜Dのラベルで唯一当たるのは `rules/shindan.md` の診断の選択肢A〜D。別物。

**何が起きるか**：受け取ったセッションは中身を確認できないので、**意味が分からないまま「違反なし」として素通りする**。実際、今回「Cの適用 ── 18本中0本。違反なし」は、Cが何かを確かめられないまま通っている。**チェックしたことになっているが、誰も何も見ていない。**

**決めること（Cocoの判断が要る）**
- この体系がどこかに実在するなら → **`rules/posts.md` に節として入れる**（「恒久ルールとして」の一言が要る）
- 実在しないなら → **指示文から外す**

**今回、中身が正典にあった唯一の項目**＝「th_15・th_21（佇まい枠）と th_10（unresolved: true）」。これは `rules/posts.md` ルール3「①②の適用外は2つだけ」に対応する。ただし**指示文は3本まとめて「5段そろわなくていい回」と書いていて、正典とずれていた**——正典では佇まい枠だけが4点全部の適用外で、**`unresolved: true` は①②だけ免除、③④は課される**。このずれのせいで th_10 の③（結果責任・証拠）の欠落が「直さないもの」に分類されていた（2026-09-02 に Coco 判断で修正済み）。

---

## ② サイト側の担当／Wチェックへ｜週データを差し替えても、古い本文が見えることがある

`assets/app.js` の `POST_V = '20260831b'` が**8/31から更新されていない**。週データの取得は `./posts/<file>?v=${POST_V}` なので、**札が変わらない限りブラウザは前の中身を出し続ける**。

- **9/1〜9/2 だけで、9/1週の本文を4回差し替えている**（実体験の反映／Xの長さ／Threadsの二択／内心の断定）。そのたびに「反映された？」の確認が要る状態になっている
- 8/31 に `?v=` の固定は一度直っている（`index.json` は `cache: 'no-cache'` になった）が、**`POST_V` を上げる運用が決まっていない**ので同じことが再発している
- **直し方は2つ**：①週データを差し替えたら `POST_V` を上げる、と運用に書く ②週ファイルも `cache: 'no-cache'` にする
- **35投稿セッションの持ち場外**（`assets/app.js`）なので触っていない

---

## ③ 35投稿セッション｜Coco待ち（9/1から継続）

`th_02`（9/1 10:00・**配信済み**）のどこを直すか。三択で Coco に出してある（①E429後半を入れる ②締めの「目盛り」を外す ③別の場所）。**推奨は②。** ①は E429 の `usage_note` ③「後半は処方の副作用側。『向かない場面』に置く」に触れるため、素材の扱いを一つ動かす判断が要る。
