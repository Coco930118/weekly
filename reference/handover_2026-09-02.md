# 申し送り（2026-09-02）

**寿命つきの文書。** 担当セッションが消化したら、その項目を削除する。**ここにルールを書かない**——ルールの正典は `CLAUDE.md` と `rules/` だけ。ここにあるのは「何が起きているか・どこを直すか・誰が」だけ。

---

## ①② は 2026-09-02 に Wチェックが消化した（記録だけ残す）

- **①指示文が正典に無い枠組みで条件を出していた** → 原因は「7段」の節を `claude/gracious-euler-5rrlsh` にだけ置いて、**`main` に上げていなかった**こと。grep が空だったのは正しい。同日 `main` へ反映した。あわせて `rules/posts.md` の免除の書き方を訂正（佇まい枠と `unresolved: true` を同じ扱いで書いていた。正典は範囲が違う）
- **②`POST_V` の上げ忘れ** → 札を外して `cache: 'no-cache'` にした。note側（`NOTE_V`）は手で上げられているのでそのまま。上げ忘れが起きたら寄せる（`reference/todo.md`）

---

## ③ 35投稿セッション｜Coco待ち（9/1から継続）

`th_02`（9/1 10:00・**配信済み**）のどこを直すか。三択で Coco に出してある（①E429後半を入れる ②締めの「目盛り」を外す ③別の場所）。**推奨は②。** ①は E429 の `usage_note` ③「後半は処方の副作用側。『向かない場面』に置く」に触れるため、素材の扱いを一つ動かす判断が要る。
