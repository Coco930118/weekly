# 申し送り（2026-09-03）

**寿命つきの文書。** 担当セッションが消化したら、その項目を削除する。**ここにルールを書かない**——ルールの正典は `CLAUDE.md` と `rules/` だけ。ここにあるのは「何が起きているか・どこを直すか・誰が」だけ。

---

## ③⑤ は 2026-09-03 に Wチェックが消化した（記録だけ残す）

- **③ th_21 の `episode_id` を E373 → E454 にした件** → **E454 のままでよい。** 直したのはツールと目視項目のほう。`full_check.py` が `episode_id == 'E373'` を直書きしていて、**佇まい枠が2本 → 0本に見えていた**（1本ではなく0本）。台帳の `band` から拾う形（`e373.tatazumai_ids()`）に変えた。あわせて `episode_check.py` の目視項目②「episode_id が E373 になっているか」も台帳から出す形にした。E373 の `elements` に「露天風呂」を追加（`rules/check.md`「要素を足すときは台帳を直す。ツールは触らない」に従う）
- **⑤ 締めの骨格が1種類しか数えられていない件** → `full_check.py` に参考カウントを追加。**「〜のは、〜まで／だけ」で数えると1本しか出ない。** 実際に収束しているのは「**〜のは、**」で主語を立てる形そのもので、**35本中13本（37%）**。上限は置かず参考カウントのみ（用量は `CLAUDE.md`「実測でしか配分を変えない」で Coco が決める）

## ④ Wチェック判定｜佇まい枠は今週1本のまま（2026-09-03）

**戻さない。9/8週の生成で2〜3本に戻す。** th_15 は Coco の添削素材 E444 で書き直したもので、佇まい枠に戻すと**受け取った素材を捨てることになる**。用量の問題は次の生成で作り込むほうが確実（`reference/todo.md` の既存判断「出来上がったものを削るより、作るときに散らすほうが自然な文になる」）。**今週は1本で通す。**

---

## B-1〜B-4 は 2026-09-03 に Wチェックが判定した（記録だけ残す）

- **B-1 わたしの一行は、読者の場面を引き継がない → 採用。** `rules/posts.md` 語り手ルールの絶対条件に追加。提案どおりの置き場所。**「行が並んだときにだけ生まれる事実」は、行単位の点検を全部通る**という指摘が核心
- **B-2 両方提示の置き場所 → 採用。** ルール3・③の中に「今日ひとつより後ろに置く／宣言の直後には置かない」を追加。**禁止の本体はルール4③のままで、③が持つのは置き場所の条件だけ**（提案どおり）
- **B-3 締めの骨格 → 採用したが、形を広げた。** 2026-09-03 に先に実装済み。**「〜のは、〜まで／だけ」で機械化すると1本しか出ない**（実測）。収束しているのは「**〜のは、**」で主語を立てる形そのもので、**35本中12本**。**上限（週5本）は置かなかった**——狭い定義での8本と広い定義での12本は別の数で、片方の数をもう片方の定義に付けると根拠が消える。参考カウントのみ（用量は `CLAUDE.md`「実測でしか配分を変えない」）
- **B-4 佇まい枠の素材 → A案を採用済み**（2026-09-03）。`episode_check.py` の目視項目②と `full_check.py` の佇まい枠カウントを、台帳の band から拾う形にした。`th_21` は E454 のままでよい
  - ⚠️ **ただし下流の指摘は、そのままでは採らなかった。** 要望書は `rules/posts.md`「佇まい枠のE373は週内重複の対象外」も**系列に開く**としていたが、**開くと場面素材の週内2回を素通りさせる**。E373 は要素の袋なので付け替えれば中身が違うが、E454 は場面なので2回出たら本当の重複。**除外してよいのは袋であって、枠ではない。** `e373.bag_ids()`（台帳で `elements` を持つ素材）で拾う形にした

## A-2 の2件目｜Threadsの「わたし」12本は、いまは動かさない（2026-09-03 Wチェック判定）

**要修正0件で、ルール違反ではない。** 機械の窓（「わたし」2回以上）に当たるのは th_13 の1本だけ。

- **今週は動かさない。** 12本のうち大半は今日の修正で**わたしの側を名乗らせた結果**で、名乗らせないと素材に無いことが紛れる（`rules/posts.md` ルール4③）。**戻すと、直したばかりの穴が開く**
- **9/8週の生成で、ゼロ主語を基本に戻す。** 1回だけ出るのは「相談＋対応例」型なので上限に数えないが、**21本中12本が持っている状態は「基本」とは呼べない**
- 用量を決めるなら実測から（`CLAUDE.md`「実測でしか配分を変えない」）。`reference/todo.md` に起票した

---

