# 診断サイトのセッション指示文（`Coco930118/shindan`）

**この4本が、診断サイト側で回す指示文の正本。** 変えるときはこのファイルを直す。

**指示文にルールを書かない。** 書き写すと二重持ちになり、必ず片方が古くなる（2026-08-30 の点検で、この形の事故が高リスク10件のうち半分を占めた）。**代わりに、正典を直接読ませる。**

**2026-08-30 にアカウントを統合したので、診断サイトのセッションからも `Coco930118/weekly` を読める。**（旧版はここを「別オーナーだから読めない」と書いていた。その前提のまま①を実行したところ、本文の入手先がどこにも無くて何もデプロイできない、という事故が実際に起きた。）**読むのは正典。作る・直すのはしない**——この2つは別のことで、読める側に倒したほうが二重持ちも貼り忘れも同時に消える。

| | 誰が | 何を持つ |
|---|---|---|
| `Coco930118/weekly` | **言葉の正典** | `CLAUDE.md` ／ `rules/type.md` ／ `rules/shindan.md` ／ `posts/shindan_*.json`（本文そのもの） |
| `Coco930118/shindan` | **サイトの実装** | データ形式・ページ・計測・デプロイ |

**ルーティンには2つのリポジトリを両方入れる。** weekly は読むだけ、shindan に書く。weekly が入っていないと①は本文を持てず、必ず空振りする。

**流れ：①weekly で言葉を決める → ②診断サイト側が weekly の JSON を読む → ③サイト仕様に落として上げる → ④Wチェック。**

---

## 共通の型（4本すべてに入れる）

```
【最重要】このセッションは**言葉を作らない・直さない**。
言葉の正典は Coco930118/weekly の CLAUDE.md ／ rules/type.md ／ rules/shindan.md にある。
**読むのはよい。作る・直すのはしない。** 正典の言葉を、サイト仕様に落として上げるだけ。
weekly のファイルは**1文字も書き換えない**（読み取り専用として扱う）。

・誤字に見えても直さない。**報告に書く**（weekly側で直す）
・表記ゆれの統一も勝手にしない
・新しい言い回しを思いついたら、実装せずに報告へ
・閾値（温度・距離のしきい値）を売上目的で動かさない

■ 出力の作法
・要修正は提案のみ。Cocoの承認を得てから確定する
・**自分でテストアクセスしたら、件数と型を必ず明記する**（実データと混ざる）
・weekly側の本文とサイト仕様が噛み合わない箇所は、必ず名指しで報告する
```

---

## ① 週次反映（水 15:00・ルーティン）

```
その週の診断を、サイトのアップ仕様に落としてください。

■ 本文の入手（この順で試す。1で足りるのが正常）
1. **`Coco930118/weekly` の `main` から、その週のJSONを直接読む**
   - X診断：`posts/shindan_x_YYYY-MM-DD.json`（`posts[]` の各要素に frame / choices / comment / shindan_url）
   - Threads診断：`posts/shindan_th_YYYY-MM-DD.json`
   - ファイル名の日付は**その週の月曜**（例：8/25週なら `2026-08-25`）
   - **`main` を読む。**作業ブランチには未マージのものが混ざる
   - **読んでいるのが本物の weekly か、先に確かめる。**ルート直下に
     `CLAUDE.md` ／ `rules/` ／ `tools/` ／ `protocol_v7.3.md` の4つがあるのが本物。
     **ルートが `assets/ index.html notes/ posts/ reference/` の5点だけなら別物**
     （6月ごろの古いフォーク。`shindan_threads_2026-06-09.json` のような旧命名も目印）。
     別物だったら**「正典が無い」と結論せず**、読んだリポジトリのルート一覧を
     そのまま報告して止まる
2. 1が見つからない・その週のファイルが無い → **Cocoに貼ってもらう**
3. 型の名前・接続の一行は、変更があった週のみ。正典は `rules/type.md`
4. **`data-wN.js` の3つは、どれも weekly に原文がある。新しく書かない**
   - **DAY_ANSWER**（7日×4型×2ドメイン）＝ X は `comment` の A/B/C/D ブロック、
     Threads は `reply_1` の4つの「・」行。**切り出すだけ。要約も言い換えもしない**
   - **TYPES**（型別の説明文）＝ `rules/type.md` の10型の表。**毎週書き直さない。**
     過去の `data-wN.js` と文言が違っていたら、直さずに**差分を報告する**
   - **配点**（選択肢→温度／距離）＝ 診断JSONの `axis_map`。
     **無ければ推測しない。**「この週の axis_map が無い」と名指しして止まる
   - 正典は `rules/shindan.md`「診断サイトの結果ページに渡すもの」

■ 本文が手に入らなかったら
**推測で埋めない。デプロイしない。**「どの週のどのファイルが無かったか」をファイル名で名指しして報告し、そこで止める。

■ 手順
1. 受け取った本文を、そのままサイトのデータ形式に落とす（**一字一句変えない**）
2. 日付ページ（/soshiki/MM-DD ／ /kankei/MM-DD）と週の対応を確認する
3. OG画像がその週のぶん揃っているか確認する
   ・**日替わりページのOGにキャラは出さない**（中身＝その日の問い／4象限マップ／固定文）。
     キャラが主役なのは**型診断（/type）の結果OG**のほう。混同しない
     （2026-08-30 訂正——旧版は「キャラ主役で統一」と書いていて、サイト側の仕様と逆だった）
4. デプロイして、実際のページを開いて表示を確認する
5. KVの計測を読む
   ・**キーは指示文に書かない。**環境変数（`KV_REST_API_URL` ／ `KV_REST_API_READ_ONLY_TOKEN`）
     から読む。無ければ計測欄に「未取得」と書いて先に進む——**反映そのものはキー無しでできる**
   ・**書き込み用トークン（`KV_REST_API_TOKEN`）は使わない。**読むだけなので読み取り専用で足りる
   ・/soshiki ／ /kankei ／ /type それぞれの **開始 / 完了 / CTA** を週別に
   ・入口ページ別の完了（どの日付ページから入ったか）／完了した型の内訳／取得時点の日時
   ・`ev`（start/complete/cta）は後から追加した項目で、**古い週は完了だけ**が記録されている
   ・**自分や関係者のテストアクセスは、件数と型を明記して分ける**
   ・母数が小さいうちは、0でも「効かなかった」とは言えない。**判定できないだけ**

■ 出力
・反映した本数と、日付ページの対応表
・落とすときに気づいた食い違い
・KVの計測（週別・ドメイン別・入口ページ別／テストと実データの内訳）
```

**この①は、上の「共通の型」とつなげるだけで貼り付け用の全文になる。**（③を参照させず手順を書き下してあるのはそのため）

---

## リポジトリの見分け方（2026-08-30 追加・実際に事故った）

**`Coco930118/weekly` の main には、必ずこの4つがルート直下にある。**

```
CLAUDE.md ／ rules/ ／ tools/ ／ protocol_v7.3.md
```

**ルート直下が `assets/ index.html notes/ posts/ reference/` の5点だけなら、それは weekly ではない。**（6月ごろの古いフォークかスナップショット。`posts/` に `shindan_threads_2026-06-09.json` のような旧命名が混ざっているのも目印）

見分けがついたら止まって、**「読んだリポジトリのルート一覧」をそのまま報告する**。「正典が存在しない」と結論する前に、まずこれを出す。**本物には 8/18・8/25・9/1 の3週ぶんの `shindan_x_*.json` と `shindan_th_*.json` が揃っている。**

---

## ② 型診断（/type）の更新（Cocoの指示・不定期）

```
間合い診断（8問の型診断）を更新してください。

■ 前提（weekly の rules/type.md で決まっていること。ここでは変えない）
・総称は「Coco Methodology 間合い診断」、通称「間合い診断」
・軸は 温度 × 距離。型は動詞人称（溶ける人・こらえる人…）で、**キャラ名を型名にしない**
・型は10（4型＋中央 × 2ドメイン）
・Coco＝見立てる／型＝立ち位置／3匹＝声、の3層。**キャラを型に1:1で固定しない**
・中央「保つ人」はCTAを出さない。代わりに**もう片方のドメインへ送る**

■ 文言の入手（この順で試す。1で足りるのが正常）
1. **`Coco930118/weekly` の `main` の `rules/type.md` を読む。**型の名前／10型の表／中央の `CENTER.crossover` はここが正典
2. そこに無いもの（一行タグ／核／④の締め／結果画面の各セクション）は、**Cocoから渡される**
3. **どちらにも無ければ、埋めない。**「どの型のどの欄が無かったか」を名指しして報告し、そこで止める

■ 反映する場所（どのファイルに）
・型の名前 → `assets/typenames.js`（エンジンはここを優先。各週の `data-w*.js` の `name:` より強い）
・10型の説明（TYPES） → 各ドメインの `data-wN.js`
・**中央の crossover → `soshiki/data-wN.js` と `kankei/data-wN.js` の `CENTER` に
  `crossover: { body, cta }` を足す**（両ドメインぶん）
  - 受け口の実装は完成済み（`assets/shindan.js` の `centerCrossover()`）。**リンク先はもう片方のハブで、実装が決める**
  - 中央のメンバーシップCTAは**すでに非表示**。`CENTER.cta` の値がデータに残っていても読まれない。**消しにいかない**

■ 手順
1. 正典の文言をそのまま反映する。**足りない箇所は空欄で残して報告する**（埋めない）
2. 結果画面の並びを崩さない。「ここまでで、下ろせるもの」は必ず残す
3. 3匹の声を置く場合は、担当ステージから出ていないか確認する
   受け止める＝しらたま／整える＝しずく／動く＝ひより。**この順番を入れ替えない**
4. 全型を1回ずつ実際に踏んで、表示・リンク・OG画像を確認する
   - **中央（保つ人）は両ドメインとも踏む。** crossover の一行とボタンが出ているか、ボタンがもう片方のハブへ飛ぶか、**メンバーシップCTAと料金表が出ていないか**
5. noindex とTOP導線の状態を報告する（公開／待機のどちらか）

■ 出力
・反映した型と、空欄で残した箇所
・全型の踏み確認の結果（表示・リンク・OG）
・**正典とサイトの文言が食い違っていた箇所**（直さずに、場所と両方の文言を並べて報告）
・**自分のテストアクセス件数と型**（KVに混ざるため必須）
```

**この②も、上の「共通の型」とつなげるだけで貼り付け用の全文になる。**

---

## ③ 計測の読み出し（週次・Wチェックの前）

```
KVの計測を読み出して、Cocoに渡せる表にしてください。

■ 出すもの（ドメイン別・週別）
1. /soshiki ／ /kankei ／ /type それぞれの **開始 / 完了 / CTA**
2. 入口ページ別の完了（どの日付ページから入ったか）
3. 完了した型の内訳
4. 取得時点の日時

■ 読み方の注意（必ず添える）
・`ev`（start/complete/cta）は後から追加した項目。**古い週は完了だけ**が記録されている
・**自分や関係者のテストアクセスは、件数と型を明記して分ける**
  （2026-08-30：w7の5件が全て同じ入口・同じ型で、テストと判明した）
・母数が小さいうちは、0でも「効かなかった」とは言えない。**判定できないだけ**

■ 出力
・上記の表
・テストと実データの内訳
・数字は weekly の reference/weekly_metrics.json に入れる（入れるのはWチェックの仕事）
```

---

## ④ 全週監査（Cocoの指示）

```
過去週のページを、いまの恒久ルールで監査してください。

■ 手順
1. Cocoから、監査の観点（禁止語・設問の主語・比喩ゼロ 等）を受け取る
2. 全週・全ページを走査して、違反を**件数と場所の一覧**にする
3. **直す前に、必ず一覧をCocoに出す**。承認を得てから反映する
4. 反映後、もう一度走査して0件を確認する

■ 注意
・**過去週のページを直すと、過去週との単純比較ができなくなる。** 監査した週と観点を必ず記録する
・「X診断は内容を絶対に変更しない」は weekly の恒久ルール。**監査で見つけても、X診断の本文は直さず報告だけ**
・配信済みの回に新ルールを遡及適用しない（weekly の「新しいルールは過去記事に遡及しない」と同じ）

■ 出力
・観点ごとの違反件数と場所の一覧
・直した件数と、直さなかった件数（理由つき）
```

---

## このファイルを置く場所

**正本は `Coco930118/weekly` の `reference/runbook_shindan.md`（このファイル）。** 診断サイト側のセッションを立てるときは、**Cocoがここから該当の1本をコピーして貼る**。

`Coco930118/shindan` 側にコピーを置かない——置くと二重持ちになり、片方が古くなる。
